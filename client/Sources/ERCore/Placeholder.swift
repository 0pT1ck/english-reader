import Foundation
import ERContract

/// Temporary: gives the target something to compile while the core is written.
/// Deleted as soon as the first real type lands.
public enum ERCore {
    public static let contractIsReachable = true
}
