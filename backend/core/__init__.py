"""Framework layer.

Everything in this package exists to make adding a feature cheap: configuration,
logging, the event bus, module registration, database access and migrations.
Feature code lives in ``backend.modules``.
"""
