"""Article generation (P1a).

Prompt construction, response parsing, and local checking. No API calls — the
manual workflow is what keeps the first verification loop small, and it lets any
model be tried, including ones with no API at all.
"""
