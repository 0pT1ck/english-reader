"""A minimal but complete feature module, kept as a working template.

Exists to prove — and to keep proving — that adding a feature touches nothing
outside its own directory. Copy this package to start a new one; delete it once
enough real modules exist to serve as examples.
"""
