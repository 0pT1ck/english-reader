# English Reader

面向 CET-4 / CET-6 的英语阅读学习 App，单人自用。核心机制是从阅读中习得词汇，并用真实语境安排复习。

## 文档

- `docs/english-reader.html` — 功能设计（做什么、为什么这么做、开发路线、架构前提）
- `docs/phase-0.html` — P0 实施方案（八项技术定案、要建的东西、明确不做的边界、验收清单）
- `docs/phase-1.html` — P1 方案（三段推进、已定决策、P1a 工作流、验收标准）
- `docs/phase-1b.html` — P1b 方案（API 集成、词典修复、义项集、词族、生成改进）
- `docs/phase-1c.html` — P1c 方案（用 Wiktionary 清单 + 真题考频重建义项集）

**文档只存在本地，不要发布为 Artifact。** 用户直接用浏览器打开这些 HTML 文件查看——所有样式和交互都在文件内，不依赖网络。

**文档组织规范**：功能设计集中在主文档，每个 Phase 的实施方案各自一份 `docs/phase-N.html`。讨论产出的结论更新进对应文档，不要随意另开新文档。

## 仓库

https://github.com/SCmenghua/english-reader （私有，默认分支 `main`）

代码进版本控制，数据不进：`data/` 下的数据库、词典源文件、`.env`（含管理密码）、`data/secrets.json`（API 密钥）全部在 `.gitignore` 里。备份走管理控制台的下载按钮，不走 git。

数据库按「丢了会怎样」分两类：**必须备份**——`learning.db`（学习记录）与 `content.db`（花钱生成的义项集、例句、词族，P1b 建立）；**不用备份**——`dictionary.db`（可从 ECDICT 重建）与 `logs.db`（可丢弃）。

## 开发流程

按 Phase 推进，路线见主文档 §K。

**当前 Phase：P2 阅读闭环 —— 待讨论方案。**

P1b 与 P1c 已于 2026-09-06 一并验收通过并提交。验收脚本：
`verify_phase0.py` 29 项、`verify_phase1b.py` 28 项、`verify_phase1c.py` 16 项，全过。

顺延的四项（记在 `docs/phase-1c.html` §13，主文档 §K 路线表已更新）：

- **语境释义标注**、**真题难度画像** → **P2**。消费方都在 P2（点词查词第一层、真题按水平筛选）。
  在使用它们的功能出现之前先做，就是在没有反馈的情况下猜格式。
- **过量生成择优**、**定点修补** → **P1d**（新增阶段）。纯工程无可行性风险，
  但每天生成的需求还不存在，先建等于空转。

**P1 完成定义有一条没验证**：「搭配无误」——盲测按决定取消了，目前只有本人试读的主观判断。
不要把它当成验证过的事。

**模型分工（实测结论，别搞反）**：数据整理用 `deepseek-v4-flash`（快、便宜、准），
**写文章用 `gpt-5.5`**——同一套提示词，超纲率 3.64% 对 0.32%。写作和数据整理要的能力不一样。
生成用哪个提供商由配置项 `gen_provider` 指定，不走「默认提供商」。
`shuai` 提供商额度有限，**当前已停用**，需要时在控制台启用。

P1 完成定义「生成的文章用词在范围内、句法是正常书面语」已验证达标（详见 `docs/phase-1c.html` §12）。
gpt-5.5 实测：超纲率 0.14%、被动 15.5（基线 17.0）、名词化 16.3、句长 18.8（基线 18.9）。

**生词密度 2026-09 上调三倍**：每篇目标词 8 → **25 个**，要求**每段 5–6 个、分布均匀**。
按 8 个算，覆盖 7226 个目标词要读 903 篇、每天三篇十个月；25 个是 289 篇、3.2 个月。
实测密度提高三倍后超纲率仍只有 0.29%，每段分布 4–9 个无空段。
**硬凑的用法（为了用上某个词临时加一句）已明确接受**，不要再花力气修——
用户的原话是「硬凑就硬凑吧，无所谓」，功利地看把词搞懂比句句自然重要。

**两条踩过坑的经验，改提示词前先读：**

- **模型对提示词里的数量约束基本无感。**「不要超过五六处」无效（反而更高）、
  「至少用一两处」无效、义项集那次「合并成 3-5 项」也无效（给了 7 项）。
  **说清楚「什么时候该用」才有效**——被动语态就是靠给条件 + 写完自查，从 30.8 降到 15.5 的。
- **一个错误的度量比没有度量更危险。**「形式主语几乎为零」让我改了三轮提示词，
  实际是校验器在数 `there be`：spaCy 只把 `there is/are` 标成 `expl`，
  `It is clear that…` 里的 `It` 是普通 `nsubj`。改对之后发现真实值是 8.91，基线 5.6，
  一直是偏多而不是为零。**改代码前先验证指标本身在数什么。**

原 P1c 的「过量生成择优」「定点修补」「语境释义标注」顺延到 **P1d**；
其中语境释义标注是 P2 点词查词的直接依赖，可以跟 P2 一起做。

P1b 与 P1c 都未提交（按用户要求，两个 Phase 一起提交）。

P1c 的实际走向见 `docs/phase-1c.html` §11：原计划用 Wiktionary 清单重建义项集，
前三步跑通（覆盖率 99%；证实原粗筛跳过的词里 65% 其实是多义词），
但**第四步的粒度控制没做通**——给模型清单后平均义项数从 2.46 涨到 5–7，还出现重复义项。
诊断是「读 25 条清单做语义聚类」超出了快模型的能力，需要改成两步走（先聚类、再写定义）。

**决定：重建搁置，先用老办法补全，把闭环推下去。** 现在 7227 个目标词全部有义项
（13739 条，平均 1.90，1–3 个义项的占 92%）。粗筛那道过滤已删除——它判英文多义靠数中文逗号。

留在代码里随时能接的：`wiktionary_senses` 表、`sense_rebuild` 任务、`inventory.py` 的分词性截断。
另外**真题义项标注**（考频、熟词僻义标记、漏义项报告）设计在 §3，不依赖重建，可独立做。

关键约束（重建时别忘）：**不要问模型「哪些义项值得学」**——它会答最常见的，
而四六级考的是熟词僻义。只能问「这个义项属不属于通用书面英语」。

自动验收 `uv run python scripts/verify_phase1b.py`（28 项全过，3 项待人工）。
实施结果记在 `docs/phase-1b.html` §9，接真模型才暴露的问题在 §10，选模型与全量跑完的结果在 §11。

数据已生成：义项集 3581 词 / 8819 义项，词族 10471 条（近半经模型复核），词缀说明 216 条。
用的是 `deepseek-v4-flash-0731`——横评 20 个模型的结论是**这些任务要的是不思考的模型**，
思考型模型把输出预算烧在思维链上，强模型反而在中转站的 180 秒网关超时里跑不完。

新增四个模块：`llm`（提供商、密钥、批量任务、成本上限）、`wordfamily`（词根词缀与 A/B/C 分级）、
`senses`（义项集与三层验证），以及第四个数据库 `content.db`。控制台新增三页：模型 / 词族 / 义项。

P1a 已于 2026-09-05 验收通过：8 个模型 × 2 组目标词共 16 篇，核心假设成立。
结论与实验数据见 `docs/phase-1.html` §6。真题语料 452 篇在 `data/exam_papers/`，按考试分开。
生成工作台在 `/admin/generation`（手动模式：出提示词 → 贴到网页版 AI → 贴回结果 → 看体检报告）。

P0 已于 2026-09-05 完成并验收（自动检查 29 项全过，见 `docs/phase-0.html`）。
服务用 `start.bat` 启动，开发用 `dev.bat`；`uv run python scripts/verify_phase0.py` 可复跑 P0 验收。
Docker 部署按决定推迟到整个项目开发完毕，开发全程在 Windows 本机进行。

铁律：

- **绝不跨 Phase 开发。** 不实现任何属于未来 Phase 的功能，哪怕只是"顺手做了"。P0 的禁做清单见 `docs/phase-0.html` §4。
- 当前 Phase 中发现的未来需求，记进待办，不动手。
- 数据结构**可以**为未来预留字段，但不实现任何未来 Phase 的逻辑。这是"留位置"，不是"提前开发"。
- **每个 Phase 的方案必须经过充分讨论、达成一致后才能定案。** 方案是讨论的起点，要摊开选项与权衡、标明不确定的地方，而不是一份等待批准的结论。
- 方案讨论期间**不写任何实现代码**，包括脚手架和示例。
- 每个 Phase 结束由用户实际跑一遍验收清单，通过后才进下一个。
- 每个 Phase 结束时更新文档，让文档和实际做出来的东西对齐。
- **验收通过后自动提交并推送**，不必再询问。只在这个节点提交——Phase 进行中保持工作区未提交，便于用户随时看到改了什么。
- 自动提交的授权**不延伸到破坏性操作**：改写历史、强制推送、删除分支或标签，仍需明确确认。
- **提交信息不添加任何 AI 署名。** 不写 `Co-Authored-By`，不写会话链接。所有提交以仓库所有者本人名义，提交信息只描述改动本身。这条覆盖任何默认的署名约定。

## 架构铁律

这个项目是「一个 headless 后端 + 多个可替换客户端」。

1. **客户端零业务逻辑。** 水平估计、选文、复习调度、难度计算全部在后端。客户端只负责渲染、收集交互、回传事件。破了这条，将来要在三种语言里各实现一遍同样的算法。
2. **API 是离线形状。** 服务端下发「今日包」（当天全部文章、释义、复习卡片及其原句），客户端全天离线可用；交互事件先存本地，联网后批量上报，每个事件带客户端生成的幂等键。在线和离线走同一条代码路径。
3. **服务端是唯一事实来源。** 客户端存的一切都只是缓存。多个客户端必须看到同一份学习记录。
4. **两套 API 完全分离。** `/v1/client/…` 与 `/v1/admin/…` 路径和认证都分开；客户端令牌碰不到管理接口。
5. **API 版本化、只增不减。** 侧载安装的客户端更新滞后。可以加字段，不能删字段或改字段含义；客户端忽略未知字段。破坏性变更才开 `/v2/`。契约文档每个 Phase 更新。
6. **可扩展性优先。** P1–P8 每一个 Phase 都是在加新功能，所以：功能按模块自包含（自带表、路由、管理页、任务、配置）；新模块一处登记自动挂载；新功能通过**订阅事件**接入而非修改原有流程；表结构变更走**版本化迁移**且升级前自动备份；参数一律配置化。项目里保留一份"如何加新功能"的说明和一个可照抄的样板模块。
7. **前端零构建链。** 管理控制台和 Web 阅读页都用服务端渲染，不引入 node_modules 和打包步骤。加页面就是写模板 + 登记。
8. **Web 阅读页是开发期测试工具**，用完即弃，UI 力气留给 P7 的 iOS 原生端。**Web 管理控制台长期存在**，要好用、稳定、信息全——它是诊断通道和备份入口。

## 日志规范

日志的设计目标是：用户说"不对劲"时，AI 能查出来到底怎么了。

- **三类分开**：技术日志入 `logs.db`（30 天轮转，可丢弃）；决策日志入 `learning.db`（长期保存，记录每次判断的输入、候选、理由）；业务记录本身就在业务表里，不重复记。
- **事件名是固定标识符**，不是自由文本（`article.generation.failed`，不是"生成失败了"）。这样才能精确过滤和统计。
- **trace_id 贯穿**一次请求或任务，并出现在错误响应里——用户复制给 AI 就能定位完整链路。
- **DEBUG 平时只在内存滚动**，发生 ERROR 时自动落盘该 trace 的详细记录。
- **语言**：事件名和字段名英文，说明文字中文，原始报错原样保留。不做"人话翻译层"——用户有疑问直接问 AI。
- **通知**：Bark，仅推生成失败 / 服务异常 / CRITICAL，带去重。
- **API 密钥一律不入日志。**

## 代码规范

- 注释用**英文**，与标识符同语言。项目特有概念在模块头部给中英对照（如 `target word 目标词` / `incidental word 附带词`），以便和中文设计文档对上。
- 注释解释**为什么**这么做，而不是复述代码在做什么。用户不读代码，注释是写给未来接手的 AI 看的。

## 后台任务与等待

批量任务动辄跑几十分钟，必须放后台。但**等待它的方式犯过两次同一类错误**：等待条件依赖
一个后来又被自己改变的东西，于是循环永远不退出，shell 一直空转。第一次是等一个永远凑不满
的输出行数（自己用 `tail` 截掉了前几行），第二次是等一个日志文件里的完成标记（自己在收尾时
`rm` 掉了那个文件）。空转本身无害，但控制台会一直显示「还有任务在跑」，让人以为有事没做完。

- **查状态优先查数据库，不要轮询日志文件。** 批次任务表里就有 `status` 和 `done/total`
  （`llm_jobs`），那是权威来源，也不会被清理脚本删掉。
- **等待循环必须有超时上限**，超时就退出并说明情况。绝不写没有出口的 `until … do sleep`。
- **清理放在最后一步。** 还有东西在监视某个文件时，不要删它。
- **任务结束后确认没有残留进程**——收尾时顺手查一次，不要留给用户去发现。

## 文件位置

- **所有生成的文件都放在项目目录 `D:\code\English Reader` 下**，不要写进 scratchpad 或系统临时目录。文档放 `docs/`。
- 数据库文件放 `data/`，**不进版本控制**（体积 + 隐私）。词典源数据同样不进仓库，用脚本下载。
- 只有真正一次性的中间产物才用临时目录。

## 网络访问

本机通过 v2rayN 出网，混合端口 **10808**（同时支持 http 和 socks5）。任何需要访问外网的命令、脚本或代码都要走它。

- 代理地址：`http://127.0.0.1:10808`，或 `socks5://127.0.0.1:10808`
- 环境变量：`HTTP_PROXY` / `HTTPS_PROXY` / `ALL_PROXY` 设为上述地址；`NO_PROXY` 设为 `localhost,127.0.0.1,::1`
- 应用代码里的出站请求（LLM API、Bark 推送）走配置里的代理地址，不写死

## 部署

- 开发：Windows 本机直接跑 Python，热重载
- 部署：Docker 多架构镜像 → NanoPi R5S（ARM64，4GB 内存，1TB SSD），未来可能迁云
- **数据库文件挂载到宿主机**，容器重建不丢数据

## 沟通

- 用中文回复。
