# English Reader

面向 CET-4 / CET-6 的英语阅读学习 App。核心机制是从阅读中习得词汇，并用真实语境安排复习。

## 文档

**为方便用户读**而做成 HTML（`docs/`，浏览器直接打开，**不要发布为 Artifact**，样式全在文件内不依赖网络）。
**第一行必须是 `<meta charset="utf-8">`**——2026-09-20 栽的：十七个文档全都没有，
文件本身是正确的 UTF-8、`file -I` 查也是 utf-8、git 里看也正常，
**只有在浏览器里打开才是乱码**，而写文档的人不在浏览器里打开它。
之前没暴露只是因为浏览器碰巧猜对了编码，换台机器就现形。
**AI 一样要读**——功能设计、每个 Phase 的决定与依据全在里面，这份 CLAUDE.md 只给指针：

- `docs/english-reader.html` — 功能设计（做什么、为什么这么做、开发路线、架构前提）
- `docs/phase-0.html` — P0 方案（八项技术定案、要建的东西、明确不做的边界、验收清单）
- `docs/phase-1.html` — P1 方案（三段推进、已定决策、P1a 工作流、验收标准）
- `docs/phase-1b.html` — P1b 方案（API 集成、词典修复、义项集、词族、生成改进）
- `docs/phase-1c.html` — P1c 方案（用 Wiktionary 清单 + 真题考频重建义项集）
- `docs/phase-2.html` — P2 方案与实施结果（§1–§12 阅读闭环、§13 P2y 词组、§18 词组召回缺口）
- `docs/phase-3.html` — P3 方案与实施结果（§2 二十八条决定、§11 模型横评、§12 实施结果）
- `docs/phase-4.html` — P4 方案与实施结果（§2 十九条决定、§4 模型配对实测、§8 实施结果与五个静默失败）
- `docs/phase-6.html` — P6 方案与结束（§3–§6 四屏共 34 条决定、§7 词组这条动了不变量、
  **§13 怎么结束的与欠下了什么——没跑验收，别把文档里的「做到的」读成「跑通了」**）
- `docs/phase-5.html` — P5 方案（§2 二十四条决定、§3 Windows 本地回路实测、§7 发件箱与服务端那个洞、
  §16 动手前先验的两个未知数、§17 验收与它的边界、**§18 Core 装什么——动手时照着这一节，不用翻前面**）
- `docs/phase-8.html` — P8 方案与实施结果（§8 开发者选项**动了铁律 4**、§9 客户端日志、
  §13 三个 Phase 一起验的清单、§16 实施结果与路上撞到的四件事）
- `docs/phase-9.html` — P9 方案与实施结果（重构）。**§17 是怎么结束的——
  代码完成而真机症状没修好，验收没通过，不记例外。先读它再读别的。****§4 的新铁律已经定案并写进这份文件
  上面那两节了**——读那两节就够，不用去 §4 找口径。方案那半边留着是为了说清「为什么」：
  §1 触发它的症状与病根、§2 那条线（服务端生文／客户端学习）、§5 五种事件、§6 多设备、
  §8 义项稳定键与那次数据大清洗的关系、§10 数据库重切、§12 验收网怎么保住。
  **实施结果在 §16，①–㉕**，动手前照那一节看「这件事上次是怎么做的」。
  **§14 那六个先验的未知数里 U1 是错的**（「用官方那个 Swift FSRS」——它在 Linux 上编不出来），
  条目标着 ✗ 并写了错在哪，别照它去找依赖。
- `docs/phase-10.html` — **P10 方案（义项大清洗，方案讨论中、未定稿）**。
  义项集整体换成柯林斯 COBUILD，全语料重标注，旧的全部归档。**词组不在这个 Phase**。
  动手前至少读三节：**§4 单词与词组的四条判据**（「单词全收、词组全不收」是用户的硬要求，
  判据穷举过 110 种标记，我在这上面漏收过两次）、**§5 义项拿什么当身份**
  （拆掉「定义的指纹」那套，它是 §7 破铁律 5 的前提条件）、
  **§12 在哪跑**（拉到 Mac 跑、跑完推回阿里云）。
  **§11 的 A0 是「先卸载重装 App」**，不是提醒。
- `docs/deploy.html` — 部署（2026-09-14/15 做的：Docker 打包、三个容器怎么摆、
  **在那台机器上构建要换三条取包的路**、§6 **备案按 SNI 掐 TLS——那个判断被实测推翻了**、
  两条入口为什么都留着、数据怎么上去以及不许怎么下来）
- `docs/testflight.html` — **给帮忙的朋友看的**，不是给我们看的：借他的开发者账号走 TestFlight 内测，
  他要做的四件一次性的事。**里面的空格是留给真实信息的，仓库是公开的，别往里填邮箱和 Bundle ID**——
  那些直接发给他。签名与上传属于下一个 Phase，这份提前写只是因为对方那边有等待时间。
- `docs/archived.html` — 归档（从主文档移出的内容，原文照录，附移出理由）。
  **不需要读它**——里面全是已经不成立的东西，读了只会把废弃口径当成现行口径。
  它只是留档备查，用户想翻旧账时自己打开；只有用户明确指名要看某条归档内容时才去读。

**这份 CLAUDE.md 每次会话整份进上下文**，所以它只放「随时可能改变下一步动作」的东西：
铁律、跨 Phase 不变量、现行配置，外加末尾那份**踩过的坑**——
那份曾经拆成单独文件，2026-09-10 又并了回来，理由见该节开头。

**文档组织规范**：功能设计集中在主文档，每个 Phase 的实施方案各自一份 `docs/phase-N.html`。
讨论产出的结论更新进对应文档，不要随意另开新文档。**实施细节写进 phase 文档，不要抄进这里**——
这份文件每次会话整份进上下文，抄一遍就是每次都付一遍钱，而且两份必然漂移。

**主文档只描述现在的系统。** 已经不成立的决定、被推翻的数字、归档的旧路线，全部移进
`docs/archived.html`，并在原位留一句指路——**移出不等于作废**，原文与实测数据照录、附移出理由。
「不等于作废」是对历史而言：**对当前开发它等同于不存在，不要去读**，主文档那句指路就是全部所需。
2026-09-10 第一次这么清的：每日配额与周期预测、「句法复杂度始终对齐 CET 真题」这句保证
（**这等于在密度与句法的取舍里选了密度**）、设计前提 03「单用户系统」（它与 P2 契约留的身份口子说反话）、
按分钟数定的时间限定，以及早已标着归档却还占着 §K 半张表的那条旧路线。
**那条旧路线的 Phase 编号一并作废**——2026-09-11 把 P5 及以后的编号从这份文件、`docs/`
（`archived.html` 除外）和代码注释里全部剔除了，没做的事一律写成事情本身、记进主文档 §M。
再看到这类编号就是漏网的：按它去找只会找到不存在的东西。

## 动手前先看末尾「踩过的坑」的对应小节

**这八种情形每一种都栽过至少一次**，其中「查词典 / 判大纲」栽了四次。

| 我正要…… | 读 |
|---|---|
| 改提示词 | §1 数量约束无感 · 错误的度量 · 绝不让模型「找错」 |
| 调模型 / 换模型 | §2 长文必开流式 · 思考型模型返回空正文 · 别拿 star 数当依据 |
| 写管理页 JS | §3 `hidden` 隐藏不掉 · 全局作用域撞名 · 用假 DOM 重放点击 |
| 加验收项或新指标 | §4 不许依赖「今天碰巧有数据」· 阴性+阳性对照 · **别把假警报推给用户** · 算法要用仿真验 |
| **跑验收** | §4.6 **它会触发真的定时任务再把它掐死**，然后把后果报成自己的失败 |
| 查词典 / 判大纲 | §5 **同一个 `tags` 字段栽了四次** |
| 写数据库代码 / 解析别处写的数据 | §6 先看一眼数据长什么样 · `IntegrityError` 必须回滚 · **时间戳格式要一致** |
| 写定时任务 / 后台任务 | §6.4 任务报成功不等于做了事 · §7.2 等待条件别依赖自己会改的东西 |
| **写 CI** | §6.6 **管道会吃掉退出码，空跑照样绿** · §6.7 分不出两种情况的断言等于没断言 |
| 读代码、等后台任务 | §7 注释描述的是意图不是实现 · 等待条件别依赖自己会改的东西 · **§7.3 查残留要按 CPU 查，别按名字猜** |
| **用户报「卡 / 慢 / 转圈」这类症状** | §7.7 **先问「我要看的东西，现在的手段看得见吗」**——看不见就别连猜三轮 |

## 仓库

https://github.com/0pT1ck/english-reader （**公开**，默认分支 `main`）

**2026-09-19 账号改名**：`SCmenghua` → **`0pT1ck`**（零、p、大写 T、数字 1、c、k——
这几个字符容易看错，照着抄）。两台机器的 remote 当天改完并实测通了。
**没受影响的**：令牌与 SSH key（绑的是账号数字 id `100364688`，不绑用户名）、
Actions 的 secrets、提交历史与贡献图（GitHub 按**邮箱**认作者，
本仓库全部是 `2059263572@qq.com`）、star/fork/issue。
CI 的三条 workflow 一个字没改——它们用的是 `github.repository` 这类上下文变量。
**唯一要记着的风险**：旧用户名立刻可被他人注册，而 GitHub 的重定向
**只在旧名没被占用时有效**。

**2026-09-12 从私有转为公开**，为的是 GitHub Actions 的 macOS runner 在公开仓库上不计费（P5/手机端要用）。
转之前扫过全部历史确认没有泄漏，扫的结果记在 `phase-5.html` §4。
**公开之后这两条要记着**：`docs/` 里的设计文档现在人人可读；仓库没有 LICENSE 文件，
默认仍是「保留所有权利」。真题在 `data/` 里没进仓库，所以主文档那条「合规卡在真题」不受影响。

代码进版本控制，数据不进：`data/` 下的数据库、词典源文件、`.env`（含管理密码）、
`data/secrets.json`（API 密钥）全部在 `.gitignore` 里。备份走管理控制台的下载按钮，不走 git。
**仓库公开之后这条从「隐私」变成了「安全」**——往 `data/` 外面写任何带密钥的东西之前先想一下。

数据库按「丢了会怎样」分类，**2026-09-18（P9 §10）从四个库改成五个**——
`learning.db` 拆没了，因为它一个文件里装着三种不同的答案，备份规则只能对整个文件说谎。

| 库 | 装什么 | 丢了会怎样 | 备份 |
|---|---|---|---|
| `content.db` | 文章、句子、token、词组、复习例句、草稿、义项集、词族 | 花钱重做一遍 | **要** |
| `events.db` | 上报的事件、词池快照、决策日志、标记与进度存档 | **补不回来** | **要** |
| `ops.db` | 设备令牌、设置、任务表、LLM 任务日志 | 重新配对加重填几个值 | 不用 |
| `dictionary.db` | ECDICT 导入 | 重新导入 | 不用 |
| `logs.db` | 技术日志，30 天轮转 | 无所谓 | 不用 |

**`events.db` 只有 0.5 MB，而拆开之前那份 42 MB 的文件对备份说的是同一句话。**
`learning.db` 留着一个 12 KB 的空壳，不删——它旁边那三个就是它的去向。
重切由 `backend/core/resplit.py` 在启动时做，幂等；守卫 `verify_phase9.py` 8.1–8.4。

## 进度

按 Phase 推进，路线见主文档 §K。**只规划当前 Phase，不提前规划未来**（2026-09-09 起）——
§K 表里只有「已完成的」和「当前这个」，发现的未来需求记进主文档 §M 待定，不动手。

| Phase | 状态 | 细节 |
|---|---|---|
| P0 地基 | 2026-09-05 验收 | `verify_phase0.py` 32 项 |
| P1 / P1a | 2026-09-05 验收 | 「搭配无误」这条完成定义**未独立验证**（盲测已取消，只有本人试读），别当成验证过的事 |
| P1b / P1c | 2026-09-06 验收并提交 | 28 项 / 15 项。义项集重建搁置，走的老办法补全，理由见 `phase-1c.html` §11 |
| P2 阅读闭环（含 P2y 词组） | 2026-09-08 验收并提交 | 自动 47 项、人工 6 项。`phase-2.html` |
| P3 复习 | 2026-09-11 验收通过 | 当年自动 42 项、人工 E 全过；D 已撤销。`phase-3.html`，`verify_phase3.py`。**2026-09-18（P9 §12）剩 30 项**——调度与队列搬到设备之后，原先的 1–4 节换成了「服务端不许再实现一遍」的迁移断言，规则本身由 CI 的 `swift test` 与 `verify_phase9` 守着 |
| P4 每日供给与今日包 | 2026-09-11 验收通过 | 自动 38 项全过、人工 M2 过；M1「断网走完一天」推迟到做手机端时验，理由见主文档 §M。`phase-4.html`，`verify_phase4.py` |
| P5 客户端内核 | 2026-09-12 验收通过 | **P5 只做 Core 与命令行验收工具，界面留给下一个 Phase**（2026-09-12 拆分）。已定二十四条见 `phase-5.html` §2：仓库转公开、只面向 iPhone 17/iOS 26、Core/界面分层、Windows 本地为主回路、本地分三类数据、发件箱一条一个文件、契约类型用生成的、状态机双保险、命令行客户端带标注、**取消加餐**。**验收全部由 AI 执行（铁律的唯一例外，见 §17）**：`verify_phase5.py` 21 项全过，内含 `swift test` 47 项；回归 P0 32 / P1b 28 / P1c 15 / P2 47 / P3 42 / P4 34 也全过（P4 少 4 项是当天复习被做完了，不是回归，见主文档 §M）。客户端在 `client/`，命令行工具 `ercli` |
| P6 界面·阅读 | 2026-09-16 验收通过（欠了三个 Phase 的账，一次还清） | 原生 iOS + 液态玻璃，**只做「列表 → 阅读 → 点词」一条链**。34 条决定见 `phase-6.html`。两条最要紧的都是减法：**正文不画系统预标的记号**（只画你自己标过的）、**界面初版不认词组**（后者动了跨 Phase 不变量，代价三笔写在 §7）。**验过的只有「能为 iPhone 17 模拟器编译」和「装得上起得来不闪退」**——列表、点词、标记、读完一次都没被真实流程走过。App 在 `client/App/`，工程文件由 XcodeGen 从 `project.yml` 生成、不进版本控制 |
| P7 界面·复习 | 2026-09-16 验收通过 | 复习那几屏搬到手机上：出题、三级提示、双向作答、当天完成判定。方案见 `phase-7.html`，22 条决定。**它推翻了 P3 的决定 18**（§3：模糊的词当天也进，但当天这一次封顶为 Hard），这是 P3 以来第一次动已定的调度规则。**明确没做的三样**：拼写那一轮、`···` 菜单、同词第二次出现的说明——前两样 P8 补上了 |
| **P8 设置与验收** | **2026-09-16 验收通过**，P6/P7 的欠账一并结清 | 设置屏八个区 + 把 P6/P7 的欠账收齐 + 三个 Phase 一起验收。39 条决定见 `phase-8.html`，实施结果见 §16。**动了架构铁律 4**：开发者选项直接控制后台（原生控件 + `X-Admin-Secret`，后端一行没改）。新建**客户端日志**（四级、一天一个文件、存三天、可导出，凭证打码）。**真机逼出十一个问题**（九个代码、一个内容、一个我的流程疏忽），
其中七个是模拟器与 CI 永远看不见的那一类——请求被取消、一秒的网络延迟、越用越慢、兜底同时落空。
全部记在 `phase-8.html` §18 |
| **P9 重构** | **代码完成，Phase 结束那一刻验收未通过；同一天搬到 Mac 后症状修好** | 用户定了**完全重构，包括改铁律**，不考虑改动大小与破坏性。那条线：**服务端只管生文，客户端拿到文、学习、把进度汇报回去**。§4–§12 全部落地，实施记录见 `phase-9.html` §16 的 ①–㉖，**怎么结束的见 §17**。架构铁律与跨 Phase 不变量已照 §4/§15 重写（上面那两节就是新的）。**自动验收 231 项零失败**、CI 的 `swift test` 116 项、两条 workflow 都绿、阿里云已重建上线。**Phase 结束那一刻真机那个症状没修好**：「快速切换选项卡就会很卡，还是会触发转圈」，推了三版（11／13／14）都还在，修掉的四个缺陷每一个都有证据但没有一个被证明是成因，**所以当时不算完成，也不记例外**。**同一天，Mac 装好之后没等下一个 Phase，当场接着查，修好了**——六层原因（老作答永久被拒、日历不是真前向重放、日期格式器/日历现建、`adopt()` 无条件重放、取消处理拉回 `.ready` 画面、`ReviewModel` 每次进屏才诞生），每一层都用 `xcrun devicectl` 连真机实测过、不是推断，细节与证据见 `phase-9.html` §17 新加的那一节「同一天后续」。**这不改判断本身**：暂停是对的决定，只是暂停的理由（看不见症状）当天就不成立了 |
| **P10 义项大清洗** | **方案讨论中**（2026-09-20 起） | 义项集整体换成柯林斯 COBUILD：单词义 25,153 条全收、词组义 4,988 条一条不收（用户定，词组单独一个 Phase）、不砍长尾、旧的全部归档连 24 条学习记录也不要。方案见 `phase-10.html`，13 节。**动了架构铁律 5**（义项 id 全变），破例的三个条件与前提写在 §7。**同日定了场地变更：服务端＝阿里云、工程＝Mac，r5s 退休**，见「开发场地」 |

**三条到期的验收断言，2026-09-15 全部改完**（当时都不是回归，别去翻旧账）。
三条都按「踩过的坑」§8 那条办了：**改成守始终成立的那一半，没删守卫，也不再天天报假警。**
- `verify_phase3.py` **3.2**——原本守 P3 决定 18「标模糊的当天不来」，而 P7 §3 推翻了它。
  现在守「模糊的当天也进，但这一次封顶」，外加 **3.4 封顶真的把评级压下来**（那一半是静默的）。
- `verify_phase4.py` **A4.1**——原本硬编码 `len(kinds) == 9`，而 P7 加翻译 worker 之后是 10 个。
  现在断言规则本身（每个 worker 都碰得到自己的开关），不数个数。
- `verify_phase1b.py` **6.1**——原本拿**旧报告里存的**超纲率跟重算的比，而两者来自不同时期的
  草稿集，old 会一路向 new 靠拢，**注定有一天会红**（撞上那天差 0.04 个百分点）。
  现在守当场做得出的那一半：派生词认得出来，且不同时出现在超纲名单里。

**第四条，2026-09-16 放宽**：`verify_phase4.py` 的 **A5.2**——原本断言「只有写文章那处
可以非默认，其余八处的思考开关保持 default」（理由是只有那处有实测依据）。
而同一天定了**十个 worker 统一走 `deepseek-flash` 并一律关思考**，十处全非默认，这条就天天报红，
而红的时候什么也没坏——那是人做的决定，不是代码跑偏。
现在守的是**「写文章那处必须是关的」**，其余几处是不是默认照实写进说明里：
**非默认不算错，但它们没有实测依据，哪天觉得某一路质量掉了先查这里。**

**还有一条不是断言的问题，同一天修的**：`verify_phase4.py` 的 **A2.4**（调度循环自己把到点的
任务跑起来）会在「循环其实好好的」时候报红——循环是**串行**的，夜间备稿只要到期就排在探针前面，
一篇文章几十秒加重试，70 秒的窗口轮不到探针。改成**等待期间先把别的任务让开，`finally` 原样还回去**。

**P3 的两项人工验收，2026-09-11 结清**（见 `phase-3.html` §12 与主文档 §M）：
D「真跑三到五天」**撤销**（验的全是体感，要在每天真的在用的场地上才量得准，那是手机端那个 Phase 的事）；
E「句子质量抽验」**通过**——105 条生成句盲判、混 15 条阳性对照、三方独立判断，查出并删除 2 条歧义句。
**结论的边界要一起记住**：三个判断者都是语言模型，共有盲区看不出来；只覆盖 7 个词（句子池只为标记过的词生成，
现在词池里 16 条）。**词一多要重验**，记在主文档 §M。

**术语**：**P2y** = P2 之内的词组工作（2026-09-07 定案、09-08 实施），不是路线图上的一个 Phase；
**P2x** 是另一回事，**已于 2026-09-07 取消**——过量生成择优降格成一条没有排期的待办，做法写在主文档 §B3。两者别搞混。

## 铁律

- **绝不跨 Phase 开发。** 不实现任何属于未来 Phase 的功能，哪怕只是"顺手做了"。P0 的禁做清单见 `docs/phase-0.html` §4。
- 当前 Phase 中发现的未来需求，记进待办，不动手。
- 数据结构**可以**为未来预留字段，但不实现任何未来 Phase 的逻辑。这是"留位置"，不是"提前开发"。
- **每个 Phase 的方案必须经过充分讨论、达成一致后才能定案。** 方案是讨论的起点，要摊开选项与权衡、标明不确定的地方，而不是一份等待批准的结论。
- 方案讨论期间**不写任何实现代码**，包括脚手架和示例。
- 每个 Phase 结束由用户实际跑一遍验收清单，通过后才进下一个。
  **现在有两次记名的例外，都不是把这条改掉：**
  - **P5，2026-09-12 定**：它没有界面，人没法「用」一个 Swift 库，人工验收也只是跑命令看输出。
    P5 的验收全部由 AI 执行，没有人工项——代价是没有第二双眼睛，见 `phase-5.html` §17。
  - **P6、P7 与 P8，2026-09-13 定、09-15 扩到三个，2026-09-16 还清**：
    验收推迟到设置也做完、一起推 TestFlight，当天三个 Phase 一次走完三十项，全过。
    **代价也如实兑现了**：九个代码问题同时到来，而它们本可以分三次遇到。
    （当天先定的是「欠到设置那个 Phase」，同日改成 P7——朋友的开发者账号当天就到位了，
    欠账因此从三个 Phase 缩到两个。**设置那个 Phase 排在这次验收之后**，不再一起欠。）
    理由是真机验一次要动用别人的开发者账号，分两次约人不如做齐了约一次；
    而模拟器上用鼠标点和真机上用拇指读判断不出同一批东西（远程操控 runner 那条路试过、走死了）。
    （2026-09-15 用户定把设置也做进这一次，于是**欠账从两个 Phase 变成三个**。）
    **代价是三个 Phase 的验收一起欠着**，塌了要一起改，见 `phase-6.html` §13、
    `phase-8.html` §1 与主文档 §M。
    **缓解只有一条，但必须守住**：每个界面 Phase 仍然必须过 CI——
    **编得过、装得上、起得来、不闪退**，这四样机器验得了。
- 每个 Phase 结束时更新文档，让文档和实际做出来的东西对齐。
- **提交只在两个节点发生，别处一律不提交：**
  1. **Phase 的方案文档彻底定稿时——问一句要不要推**（不自动推，等回答）。
     加这一条的原因是方案讨论常常跨天跨会话，文档定稿了却不落库，下次开会就得靠翻对话重建。
  2. **验收通过后自动提交并推送**，不必再询问。
  除这两处之外，**Phase 进行中保持工作区未提交**，便于用户随时看到改了什么。
  **原来还有第三个节点**（2026-09-14 加、2026-09-19 撤销）：「本地不装 Swift，
  改完 Core 必须提交并推 `ios` 才能靠 CI 验证」——那条的前提是本地验不了 Core，
  而 Mac 迁移之后前提没了（见「开发场地」），本地跑一遍
  `scripts/ci_client.py` 就知道改得对不对，不用再靠提交触发验证。
- **界面的提交走 `ios` 分支，验收通过后合回 `main`**（2026-09-13 定，2026-09-16 第一次兑现）。
  P6/P7/P8 的提交在 `ios` 上攒了三个 Phase，TestFlight 验收通过当天一次合回 `main`。
  **下一个界面 Phase 照旧**：先在 `ios` 上攒，验收通过再合。
  看到 `main` 落后不要去「修」它——那是有意的。
- 自动提交的授权**不延伸到破坏性操作**：改写历史、强制推送、删除分支或标签，仍需明确确认。
- **提交信息不添加任何 AI 署名。** 不写 `Co-Authored-By`，不写会话链接。所有提交以仓库所有者本人名义，提交信息只描述改动本身。这条覆盖任何默认的署名约定。

## 架构铁律

**2026-09-18（P9）整体重写过一次。** 这个项目是「一个**内容工厂** + 一个**学习者**」：
服务端生文、存档、送货；客户端学习，并把进度汇报回来好让工厂接着生。
旧的那套写的是「headless 后端 + 可替换客户端」，而那句话把学习记录也算进了后端——
症状见 `phase-9.html` §1（复习页转圈、刷新几次才回到 13/13），根因见 §2。
**每条都带验法：一条没有验法的规矩只是一句愿望**（P8 §8 动铁律 4 那次学到的）。

1. **服务端不许有任何学习逻辑。** 排期、队列组成、选文、进度聚合，一处都不许有。
   守卫 `verify_phase9.py` 第 3 节（读源码断言）。
   **替掉旧铁律 1「客户端零业务逻辑」**——它的理由是「将来要在三种语言里各实现一遍」，
   而现在只有一份 Core（Swift），iOS App 与 `ercli` 共用它。
   服务端也根本不需要 FSRS：夜间备稿从学习记录里只要一个「正在学的词」的集合。
2. **客户端不许产出内容，服务端不许解释记录。** 两个方向都要说：
   前半句防「顺手在手机上拼个句子」，后半句防「管理台顺手算一下他学得怎么样」。
   守卫 `verify_phase9.py` 3.x 与 `verify_phase3.py` 1.1–1.10。
   **唯一的记名让步在 §7**：服务端要知道「哪些词在学」才能保证每篇有生词——
   而它是靠**客户端上报快照**拿到的，不是靠自己推，所以不算破例。
3. **API 是离线形状。** 服务端下发当天要用的内容（文章全文、释义、在学词的句子池），
   客户端全天离线可用；交互事件先入本地日志，联网后批量上报，每条带客户端生成的幂等键。
   在线和离线走同一条代码路径。
4. **屏幕上没有一个数字等网络。** 每一个显示出来的数都要有本地来源。
   **这是铁律 1 的可执行版本**，也是整个 P9 的起因：13/13 变 0/13 是因为那两个数来自服务端。
   守卫 `verify_phase9.py` 第 5 节。
5. **内容 id 一旦发出去就不许变。** 文章、句子、token、义项的 id 是契约的一部分。
   **重新分析必须是加一个新版本，不是原地重建。**
   以前这只是实现细节（坑 §6.5 只伤服务端自己）；记录搬到设备之后，
   标记指着的正是这些 id，**id 一变就是别人手机上的记录静默失效**。
   义项那一半 P9 §8 做掉了（稳定自然键 + 退休表，不再删了重插）。
6. **两套 API 完全分离。** `/v1/client/…` 与 `/v1/admin/…` 路径和认证都分开；
   客户端令牌碰不到管理接口。
   **2026-09-15 有一条记名的例外**：手机端的**开发者选项**直接调管理接口，用的是另一把凭证
   （管理密码走 `X-Admin-Secret`，后端一行没改——`require_admin` 本来就接受它）。
   设备令牌照旧碰不到管理接口，破掉的是「管理只在控制台发生」那半句。
   **例外不延伸**：只在开发者选项那一页背后、只用那把凭证；阅读、复习、设置的正常路径上
   一个管理调用都不许出现。理由与边界见 `phase-8.html` §8。
7. **API 版本化、只增不减。** 侧载安装的客户端更新滞后。可以加字段，
   不能删字段或改字段含义；客户端忽略未知字段。破坏性变更才开 `/v2/`。契约文档每个 Phase 更新。
   **2026-09-18 有一条记名的放宽，只此一次、只在状态/同步那半边**（P9 §11）：
   `GET /reviews`、`GET /reviews/calendar`、`POST /reviews/answer` 删掉了。
   它们是「服务端跟着用户的拇指改学习状态」的最后几处，而那正是这个 Phase 拆掉的东西；
   它们唯一的调用方（Web 复习页）在同一个 Phase 一起删了。
   **内容那半边照旧只增不减**，`TodayResponse.reviews` 这类字段留着不删就是为了守住它。
8. **可扩展性优先。** 每一个 Phase 都是在加新功能，所以：功能按模块自包含
   （自带表、路由、管理页、任务、配置）；新模块一处登记自动挂载；
   新功能通过**订阅事件**接入而非修改原有流程；表结构变更走**版本化迁移**且升级前自动备份；
   参数一律配置化。项目里保留一份"如何加新功能"的说明和一个可照抄的样板模块。
   **这套东西正是让 P9 这次搬家做得动的原因**，一个字没改。
9. **前端零构建链。** 管理控制台用服务端渲染，不引入 node_modules 和打包步骤。
   加页面就是写模板 + 登记。
10. **Web 管理控制台长期存在**，要好用、稳定、信息全——它是诊断通道和备份入口。
    **但它看的是副本，不是原本**（2026-09-18 加）：学习记录的第一副本在设备上，
    控制台看到的是上报上来的那一份。所以它该显示「设备说什么／服务端存档说什么」两列，
    而不是合成一个数——两列对不上正是「这台设备有一阵没同步了」的唯一可见信号。
    **Web 阅读页与 Web 复习页已于 2026-09-18 删除**（P9 §11）：
    新架构下它们要能学习就得在 JS 里再实现一遍学习引擎，而那正是铁律 1 禁止的事。
    第二客户端是 `ercli`，它走同一份 Core。

## 跨 Phase 不变量

改动时别破。破了这些的后果都是**静默的**——没有报错、没有日志，末尾「踩过的坑」§8 有一份症状清单。

**2026-09-18（P9）：前三条的执行点从服务端换到了设备，语义一个字没改。**
现在它们由 `ERCore/Projection.swift` 的重放实现，守卫也搬了家——
服务端那半边由 `verify_phase3.py` 1.1–1.10 守着「不许再实现一遍」。

- **记账只在读完那一刻发生。** 入库不动词池，打开文章不动词池。
  现在由投影保证：只有 `article.finished` 那条事件会加遇见次数。
  守卫 `verify_phase2.py` 7.1（服务端那半）与 `verify_phase9.py` 4.x。
- **进复习队列只有一条路：你自己标记。** 2026-09-11 收紧——原本还有第二条（读后抽查答错），
  **读后抽查整套已决定不做并归档**（主文档 §D 留桩、§J 新增一条边界）。不认识却不标记＝不想学，系统不替你查证。
  推论：**超纲词也能标、也能进队列**——「不在大纲里」是关于大纲的事实，「不许学」是替用户做主。
  不计生词率照旧（那个数描述的是文章对大纲，不是文章对你）。
  **读完一篇文章只增加遇见次数，不改变词池位置**，不管是真题还是为教那些词而写的生成文。
  试过两版自动记账，都推翻了。守卫 7.4。
- **撤销标记后条目退回 `new`，但遇见记录保留**（它确实被遇见过）。守卫 7.13。
  **P9 起这条自己成立了**:撤销就是日志里的一条 `word.unmarked`，下一次重放它自然不在队列里。
  原先要靠一条订阅去「收掉今天那道题」，而那条订阅在 2026-09-16 之前只做了一半（坑 §7.1）。
- **同一天同一张卡，问过几次是累加的。** 那个数就是成绩（`asks`），不是「最近一次怎么样」。
  答错退回第一向、权重减半，但计数不清零——否则反复答错会显示成一次干净的通过。
  守卫 `review-vectors.json` 十个场景（由 `scripts/review_reference.py` 导出，两种语言对拍）。
- **内容 id 一旦发出去就不许变。** 见架构铁律 5。这条同时是不变量，
  因为破它的后果是**别人手机上的记录静默失效**，而这边什么也看不到。
- **词组是一个整体，就按一个整体渲染**——这条对所有客户端都成立，将来的原生端也算。
  静止状态正文里不加记号（下划线三种、底色两种已占满），但**悬停、点选、标记都整体发生**，
  一处词组渲染成一个元素，两个词连同中间的空格都在里面。静止要不要标出来是各客户端自己的呈现决定，
  「选中与标记按整体」不是。
- **契约里给未来留位置，值先为空**：水平估计、义项的记忆状态、专有名词说明这类将来某个 Phase 才填的东西，
  字段在契约里就要有位置，并由响应顶层的 `capabilities` 声明区分「没数据」和「没实现」。
  但**只留形状显而易见的位置**（布尔、一句话、一个数、可整体为 null 的对象）；
  复习卡片、今日包这类结构复杂的不猜，将来以**新增端点**接入。详见 `phase-2.html` §6。
  身份也是这么留的：由设备令牌推导，响应顶层回显 `learner`，路径里不出现人的编号。
- **换义项集时不要重新标注。** **P9 §8 把这条的实现修好了**：`store_senses` 现在按
  稳定自然键 upsert，退休而不删除，同一个键永远是同一个 id（13,741 条已回填）。
  但规矩本身留着，因为它描述的是**后果**：一次全量替换若让 id 变了，会静默作废
  93,041 条语境标注（460 万输入 token）和全部真题考频。
  `senses.replaced` 事件是给**单词补全**用的，不是给换词典用的，别搞混。
  **用户定了将来要做一次数据大清洗**——「已经定了的义项会全部更换」，外加把词组补齐。
  **「全部更换」这四个字和上面这条必须一起读**：走稳定键那条路它就是安全的，绕开它就不是。
  动因、三层根因与实测规模（2400 处 token、`account` 六成出现在词组里）记在主文档 §M。
- **`is_exam_key` 熟词僻义标记保持为 0。** 规则本身成立，但它依赖可信的义项排序，而现在的序号是模型猜的
  （按它会把 `well`「好」、`even`「甚至」标成僻义，占全部义项的 19%）。理由见主文档 §F4。
- **不要问模型「哪些义项值得学」**——它会答最常见的，而四六级考的是熟词僻义。
  只能问「这个义项属不属于通用书面英语」。

## 模型与中转站

- **只有一条路能调通模型：`gemini-3.8-flash-high`**（提供商 `gemini`，自建网关
  `gcli2api.cfoptick.com`，免费）。**2026-09-20 起只有阿里云一台**，
  `secrets.json` 里只有 `gemini` 一个 key，`llm_providers` 表里**只有 `gemini` 一行**
  （中转站那几个提供商的行在这台上已经不在了）。
  - **阿里云现行配置**（2026-09-20 实查）：`gen_provider = gemini`、
    `llm_provider_generate_article = gemini`，**其余九个 worker 没有单独配**，
    走「默认提供商」也就是 `gemini`；`llm_concurrency = 3`、
    `llm_requests_per_minute = 18`；夜间备稿 `task_generation_daily_enabled = true`、
    `04:00`。
  - **两处指着不存在的东西，动手时先修**（都是从两台合一留下来的）：
    ① **`llm_thinking_generate_article = default`**，而 `verify_phase4` A5.2 守的是
    「写文章那处必须是关的」——**这条现在会报红**，应改成 `off`
    （那个网关本来就忽略这个参数，关着零成本）；
    ② **`review_translate_provider = dsflash2`**，而那个提供商在这台上根本没有行、
    也没有 key——**复习翻译那一路现在是断的**，改成 `gemini`。
  - **夜间备稿现在可以安心开着**：从前两台各自 04:00 生成会让 `content.db` 静默分叉，
    所以 r5s 那份一直故意关着；**现在只剩一台，那个理由消失了**。
    但**做数据大动作时仍然要先关**（比如 P10 的全语料重标注，
    不关它就是一边清洗一边进新文章，把靶子搞成移动的）。
  - **它必须配 `p1b-2` 提示词才能用**：同一个模型，旧提示词 0/10 合格、超纲中位 5.82%，
    新提示词 7/10、0.56%，与 gpt-5.5 当年的 8/10 / 0.39% 基本持平。
    **差别全在提示词上**，不在模型上。五版实测与三条要打的折见 `phase-4.html` §4 末节。
  - **关思考对它无效**：那个网关忽略 `enable_thinking`，拨哪边结果都一样。
    「切模型必须关思考 ＋ 给足预算」那条是对中转站的 deepseek 说的，**别照搬到这里**。
  - **造例句、标注这几路没测过**，只是跟着默认提供商走到了它身上。质量存疑先查这里。
- **2026-09-15 那天定的「十个 worker 全部指向 `deepseek-flash`（`dsflash2`）、思考一律关」
  已经不存在了**：key 早删了，中转站余额一直没充，那条路仍然是 402。
  **`review_translate_provider` 是这次合并后唯一漏网的，还指着 `dsflash2`**（见上）。
  下面这两段分工留着不删，是为了哪天充值之后改得回去——**但那时要先重新填 key**。
  - **关思考这一条只有写文章有实测**（2026-09-11，十个相同种子：5/10 → 8/10，61.6s → 9.1s）。
    另外九个是**按横评结论推的**——「数据整理要的是不思考的模型，思考型模型把输出预算
    烧在思维链上」。**推的不是测的**，哪天觉得某一路质量掉了，先怀疑这里。
  - **造例句这一路要留意**：横评里 `deepseek-v4-flash-0731` 96% 高于 `gpt-5.5` 94%，
    而现在换成了另一个模型（`deepseek-flash` 与 `deepseek-v4-flash-0731` **是两个不同的模型**，
    fingerprint 不同），没测过。
- **改之前的分工，改回来时照这个填**：数据整理用 `deepseek-v4-flash`（快、便宜、准），
  **写文章用 `gpt-5.5`**——同一套提示词超纲率 0.32% 对 3.64%。
  **但「给一个义项造例句」不适用这条**，见「踩过的坑」§2.4。
- **长文生成必须开流式**（`llm_stream`，默认开）。关掉它长文生成直接不可用，原因见「踩过的坑」§2.1。
- **中转站限速 20 rpm**，这条比模型延迟更决定批次总耗时。
  `seed-2.1-*` 与 `qwen3.8-max` 全部 504，`minimax-m2.7` 全部 429。
- **2026-09-15：两条路当天都断了，实测。**
  - **基元律动（`tokenrhythm.studio`）余额不足**，HTTP 402 `INSUFFICIENT_BALANCE`。
    `dsflash`（`deepseek-v4-flash-0731`）与 `dsflash2`（`deepseek-flash`）各测一次，**都是 402**——
    因为 **`secrets.json` 里那四个条目是同一把 key**（`1`/`dsflash`/`mimo`/`judge`/`dsflash2`，
    sha256 一致），**换 provider、换模型都换不出余额来**，它们是同一个账户。
    （`mimo` 与 `judge` 对应的 provider 早已不在 `llm_providers` 表里，是孤儿条目。）
    **这六个条目 2026-09-18 全部删掉了**，所以现在连「试一次看还是不是 402」都做不到——
    要试得先重新填 key。
  - **ShuaiAPI（`api.shuaiapi.com`，`gpt-5.5`）HTTP 400**：
    `The channel is temporarily unavailable. Please contact the administrator.`
    **不是余额，是它那边的上游通道**。
  - 所以那一天**没有任何一条调得通模型的路**。
  **一切要花钱的动作都跑不通**：生成、翻译、判词组、标注。
  由此带出的连锁**都不是回归，别去查代码**：`verify_phase2` 7.9（词组判不完）、
  `verify_phase4` B5.1／B5.2（文章卡在 `annotating`、词组待判）。
  **2026-09-16 补记**：ShuaiAPI 那条当天**自己好了**（通道恢复），但账上只剩 ¥0.035，
  写了两篇就 403 `insufficient_user_quota`，**等于没有**。中转站仍是 402。
  所以现在能调通的只有 gemini 那条免费的路。
  **由此那一串「不是回归」的连锁 2026-09-18 就断了**：`verify_phase2` 7.9、
  `verify_phase4` B5.1／B5.2 现在真的能过——调通模型之后，
  卡在 `annotating` 的那篇当场治好了（走 `daily._resume_unfinished`，治文章不治任务）。
- 生成用哪个提供商由配置项 `gen_provider` 指定，**不走「默认提供商」**。
  `shuai` 提供商额度有限，**2026-09-16 已耗尽**（余额 ¥0.035，两次调用就 403）。
- **模型名是 `deepseek-flash`**（不带版本不带日期，DeepSeek 官方改的，中转站跟着改）。
  写 `deepseek-v4.1-flash` 会得到 `MODEL_NOT_AVAILABLE`——**不是没放开，是改名了**（2026-09-11 订正）。
  写文章实测**关思考 8/10、与 gpt-5.5 打平且快 8.5 倍，开思考只有 5/10**——要用必须关。
  **「`client.complete()` 不接受这个参数」这句 2026-09-15 订正：它接受**——
  P4 把思考开关做成了每个 worker 一个配置项（`llm_thinking_<kind>`，取 `on`/`off`，
  别的值＝不发这个参数、随模型默认），`client.complete(..., thinking=)` 也直接收。
  当时写下那句之后功能就做了，而这里一直没跟上（坑 §7.1 的同一个形状）。
  **2026-09-13 实测补一条**：`deepseek-flash` 与默认提供商配置里那个 `deepseek-v4-flash-0731`
  **都回 200，而且是两个不同的模型、不是别名**（fingerprint 不同）。失效的只有 `deepseek-v4.1-flash`。

## 日志规范

日志的设计目标是：用户说"不对劲"时，AI 能查出来到底怎么了。

- **三类分开**：技术日志入 `logs.db`（30 天轮转，可丢弃）；决策日志入 `learning.db`（长期保存，记录每次判断的输入、候选、理由）；业务记录本身就在业务表里，不重复记。
- **事件名是固定标识符**，不是自由文本（`article.generation.failed`，不是"生成失败了"）。这样才能精确过滤和统计。
- **trace_id 贯穿**一次请求或任务，并出现在错误响应里——用户复制给 AI 就能定位完整链路。
- **DEBUG 平时只在内存滚动**，发生 ERROR 时自动落盘该 trace 的详细记录。
- **语言**：事件名和字段名英文，说明文字中文，原始报错原样保留。不做"人话翻译层"——用户有疑问直接问 AI。
- **通知**：Bark，仅推生成失败 / 服务异常 / CRITICAL，带去重。
- **API 密钥一律不入日志。**

## 代码规范

- 注释用**英文**，与标识符同语言。项目特有概念在模块头部给中英对照（如 `target word 目标词` / `incidental word 附带词`），以便和中文设计文档对上。
- 注释解释**为什么**这么做，而不是复述代码在做什么。用户不读代码，注释是写给未来接手的 AI 看的。
  但**注释描述的是意图，不保证实现跟上了**——读代码时别拿它当实现的描述，见「踩过的坑」§7.1。

## 后台任务与等待

批量任务动辄跑几十分钟，必须放后台。等待它的方式犯过两次同一类错误，见「踩过的坑」§7.2。

- **查状态优先查数据库，不要轮询日志文件。** 批次任务表里就有 `status` 和 `done/total`
  （`llm_jobs`），那是权威来源，也不会被清理脚本删掉。
- **等待循环必须有超时上限**，超时就退出并说明情况。绝不写没有出口的 `until … do sleep`。
- **清理放在最后一步。** 还有东西在监视某个文件时，不要删它。
- **任务结束后确认没有残留进程**——收尾时顺手查一次，不要留给用户去发现。
  **按 CPU 查，别按进程名猜**（`Get-Process | Where-Object { $_.CPU -gt 60 }`）：
  名字是猜的，而跑飞的往往正是那条你以为无关的一次性命令。
  2026-09-12 栽过一次，见「踩过的坑」§7.3。**另：Windows 上别写 `find /`**，它会扫遍所有盘。

## 开发场地

**2026-09-20 用户定：服务端就在阿里云，工程就在 Mac，r5s 退休。**
在这之前是「Python 后端在 r5s、Swift 在 Mac、阿里云跑开发期实例」三处分工，
现在收成两处。**r5s 上那份 `data/` 从此是一份过期拷贝，不要再从它上面取数。**

**动手前实测过两边分叉没有**（2026-09-20）：r5s 一直挂着「数据主副本」的名分，
而生产方向是反的——夜间备稿从 09-16 起跑在阿里云，新文章只在那里产生。
文章 id 逐个做集合差：**阿里云有而 r5s 没有的 20 篇，r5s 有而阿里云没有的 0 篇**。
**阿里云是 r5s 的严格超集，所以没有搬迁，也没有合并。**
（阿里云 498 篇 / 235,408 token / 27 条标记，r5s 那份是 478 / 225,231 / 24。）

**Swift / iOS 那一侧 2026-09-19 就迁到 Mac 了**，理由是 P9 结束时那笔账
（`phase-9.html` §17）：真机上「切选项卡卡 ＋ 转圈」推了三版没修好，
三版的形状一模一样——**远程读日志 → 推断 → 出一版 → 症状还在**。
事后的日志能证明「某个请求失败了」，证明不了「屏幕为什么在转」。
有了 Mac 才量得到主线程耗时、才看得见切屏时 SwiftUI 重建了什么、`.task`
被取消了几次，而 `swift build` 也从「推一次等 CI 一分钟」变成本地几秒。
**P6 §13 当年写「远程操控 runner 那条路试过、走死了」，这条路现在通了。**

**Swift/iOS 那一侧不再靠 CI 才能验证**（2026-09-19 定）：`scripts/ci_client.py`、
`scripts/ci_app.py`、`scripts/ci_testflight.py` 三份脚本照着 `client.yml`、
`app.yml`、`testflight.yml` 各自的步骤原样搬了一遍，本地跑，比推一次等 CI 快得多。

| 活 | 在哪 |
|---|---|
| **代码、文档、全部开发** | **这台 Mac** |
| **服务端运行、数据主副本、夜间备稿** | **阿里云**（跑的是 Docker 镜像，不是源码——见下面那条） |
| Swift / SwiftUI 的编写、`swift test`、Core 给 iOS 编译、契约生成比对、App 编译装模拟器、TestFlight 归档签名上传 | **这台 Mac**（`uv run python scripts/ci_client.py` / `ci_app.py` / `ci_testflight.py [--upload]`） |
| 上面这些的独立第二次验证 | **CI**（`client.yml` / `app.yml` / `testflight.yml`） |
| r5s | **退休**（2026-09-20）。数据是过期拷贝，别取；机器还在，但本项目不用它 |
| Windows 那套 | 保留，**备用的备用** |

**改了服务端代码，阿里云不会自己变**——它跑的是镜像。发版路径是
**git push → 那台 `git pull` → 重新构建镜像 → 重启容器**，照 `/root/build-er.sh`
（三条国内源写在里面，重建几十秒，代码层在 Dockerfile 最后、依赖层命中缓存）。
**判断有没有上去别靠记忆**：
`docker exec english-reader grep -c <你刚加的函数名> /app/backend/...`，答案是 0 就是没上去。
这条坑 2026-09-16 栽过一次，详见「部署」那节。

**数据在阿里云，而开发在 Mac，所以要动数据的活有两种走法**：
① **小改、看一眼**——`ssh aliyun` 直接在那台上查；
② **大动作**（比如 P10 的义项清洗，要反复调解析器）——
**关掉夜间备稿、手机先别用，把库拉到 Mac 跑，跑完推回去、重建镜像、开回备稿**。
两种走法都要守住那条老规矩：**任何时候只有一份在被写。**

**CI 还在，一个字没改。** `client.yml` 里 Core 在 `swift:6.3` 的 Linux 容器里
跑 `swift test` 那半份，本地 Mac 验不了（Core 在 Linux 上编不编得过是本地
macOS 环境回答不了的问题）——所以 CI 不是被替换，是从「唯一能验的地方」
降级成「独立的第二次验证」，这一半的覆盖仍然只有它能给。

**云签名以前在 CI 上每推一次就烧一张证书，根子不在签名方式，在「CI 的钥匙串
每次都是空的」**——本地钥匙串是持久的，Xcode 认得已经建过的证书就直接用，
不会再问 Apple 要一张新的（2026-09-19 实测：本地连跑两次归档，第二次原样
复用了第一次建的那张）。签名凭证放在 `data/signing/`（整个 `data/` 都在
`.gitignore` 里）——和 CI 用的那份 GitHub secret 是**同一把**App Store Connect
团队密钥的两份拷贝，不是另建的。`ci_testflight.py` 默认只导出到本地，
要真的传上 TestFlight 得显式加 `--upload`（那是号主和测试员都看得到的外部动作，
脚本的默认值就是唯一的关卡，没有 Actions 页面那道「点 Run 之前看一眼参数」）。

**Core 交叉编译踩过一个坑，记这儿免得下次又撞上**：`client.yml` 写的
`-Xswiftc -target ... -Xswiftc -sdk ...` 那套在这台 Mac 的 Xcode 27 上
**不生效**——会静默编出 macOS 的产物，不报错，只是 Mach-O 里的平台标记不对，
`otool -l` 读出来是 `platform 1` 不是 `2`。CI 锁的是 Xcode 26，这大概率是
两个版本间 SwiftPM 处理跨平台编译目标的差异。本地要用 `--triple` **加**
`--sdk` 这两个顶层参数（缺一个都不对），`ci_client.py` 已经改成这个写法；
**CI 那份先不动**，Xcode 26 上原来那套还是好的，等哪天升级 CI 的 Xcode
版本再一起核实。

**契约类型现在本地也生成得出来**（`python3 scripts/generate_swift_types.py`，
生成器要 `swift run`，这台 Mac 有 Swift 工具链）。2026-09-19 用这条本地路径
逮到一次真的漂移：`client/openapi.json` 在 `4aac095` 那次改了批量端点的入参
形状，但生成的 Swift 类型没跟上——r5s 编不出来、也没人推一次去触发 CI 的
`regenerate` job，漂了好几个提交都没被发现。**别手改 `client/Sources/ERContract`**，
那是生成物；改完契约，本地跑一遍 `scripts/ci_client.py`（或单独跑生成器）
再提交，不用再等 CI。

**`verify_phase5.py` 的 Swift 部分**（内含 `swift test` 47 项）以前在 r5s 上跑不了，
**这台 Mac 能跑**——但那个脚本本身还没改成认本机的 Swift，跑之前先看一眼它认不认得。

**连法**：`ssh aliyun`（`~/.ssh/config` 已配，Mac 上的密钥是 `~/.ssh/id_rsa_aliyun`），
`47.116.104.62`，用户 `root`。域名 `reader.cntick.top`。
数据在 **`/opt/appdata/English reader/data/`**（注意目录名里有空格），
挂进容器的 `/app/data`。

**曾经的两个场地，留一句交代**（细节已随它们退休，不再占地方）：
Windows（`D:\code\English Reader`）2026-09-14 让位给 r5s；
r5s（NanoPi R5S，`/data/code/English`）2026-09-20 退休，让位给「阿里云 ＋ Mac」。
两台机器上的 `data/` **都是过期拷贝，不要再启动服务、不要再从上面取数**。
搬走的理由每次都一样：**任何时候只有一份在被写**——而现在被写的那份在阿里云。

## 文件位置

- **所有生成的文件都放在项目目录下**，不要写进 scratchpad 或系统临时目录。文档放 `docs/`。
  Mac 上是 **`/Users/optick/Documents/code/english-reader`**；
  阿里云上代码在 `/opt/appdata/English reader/`，数据在它下面的 `data/`
  （**目录名里有空格**，写命令记得引号），挂进容器的 `/app/data`。
- **数据库所在的盘要有 POSIX 文件锁语义**——SQLite 靠它，挂 exFAT 或 NTFS 的盘会出很难查的问题。
  Mac 的 APFS、阿里云的 ext4 都没问题；这条是留给「哪天想把库放到移动硬盘或网络盘上」的人看的。
- 数据库文件放 `data/`，**不进版本控制**（体积 + 隐私）。词典源数据同样不进仓库，用脚本下载。
- 只有真正一次性的中间产物才用临时目录。

## 网络访问

**Mac（主场地）：`http://127.0.0.1:10808`**，环境变量已经导好
（`HTTP_PROXY` / `HTTPS_PROXY` / `ALL_PROXY` / `NO_PROXY` / `no_proxy`）。
2026-09-20 实测：GitHub、PyPI、`api.anthropic.com` 都通，freemdict 那类下载站直连也通。

- **`NO_PROXY` 和 `no_proxy` 在这台机器上两个都设着，而这是安全的。**
  「踩过的坑」§7 那条「绝不两个都设」**只对 Windows 成立**——那边环境变量不区分大小写，
  Foundation 建字典时会撞重复键、进程当场没（`Fatal error: Duplicate values for key`）。
  **macOS 和 Linux 区分大小写，是两个不同的键，不会撞**——这台机器上就是两个都设着，别来「修」它。
  这条在主场地搬到 Mac 之后要紧了：`ercli` 和 `swift test` 都在这台机器上跑。
- **阿里云**：那台有 mihomo，`http://127.0.0.1:7890`。**拉 GitHub 必须挂它**——
  直连报 `GnuTLS recv error (-110)`，`https_proxy=http://127.0.0.1:7890 git pull` 就过。
  这跟「别给 Docker 守护进程配代理」不冲突：那条说的是构建时拉包（见「部署」）。
- 应用代码里的出站请求（LLM API、Bark 推送）走配置里的代理地址，不写死。
- **Claude Code 不支持 SOCKS**，10808 和 7890 都是 http 混合口，正好。

## Swift 工具链（P5 起；2026-09-19 起主路径是 Mac，这套 Windows 的再降一级）

**主路径现在是这台 Mac**（Xcode 27 自带 Swift 6.4），见「开发场地」。
下面这套留在 Windows 上应急——原来是「Windows 备用、CI 主路径」，
现在 Mac 顶了 CI 的位置，Windows 排到了第三。
（当年还有一条「r5s 上不装 Swift」，那台 2026-09-20 退休了，这条跟着作废。）

2026-09-12 装在 **`D:\Software\Swift`**，Swift **6.3.3**，target `x86_64-unknown-windows-msvc`。
靠 VS Community 2026（MSVC 14.51.36231）与 Windows SDK 10.0.26100.0 编译链接——
**官方文档写的前提是 VS 2022，这套更新的组合实测能用**。

**新开的终端已经带好了环境**（安装器写进了用户 PATH 和 `SDKROOT`）。只有在环境变量写入之前
就启动的 shell 才要手动补，那时两样都要，缺一样都失败得很难看：

- PATH 要**两个目录**：`Toolchains\6.3.3+Asserts\usr\bin` 和 `Runtimes\6.3.3\usr\bin`。
  只加第一个的话 `swift --version` **什么都不输出，退出码也正常**。
- `SDKROOT=D:\Software\Swift\Platforms\6.3.3\Windows.platform\Developer\SDKs\Windows.sdk`，
  不设就报 `unable to load standard library`。

**Core 的每个依赖都要单独核实能不能在 Windows 上编**，别默认「Swift 生态的应该都行」。
已验过：`swift-openapi-runtime` 1.12.1 + `swift-http-types` 1.8.0、生成器本身（产出能编译能解码）。

**跑任何 Swift 程序前，代理环境变量有两条要守**（2026-09-12 各栽一次，详见 `phase-5.html` §16）：

- **只设大写的 `NO_PROXY`，绝不 `NO_PROXY` 和 `no_proxy` 都设。**
  **这条只对 Windows 成立**：Linux 环境变量区分大小写，两个是不同的键，不会撞
  Windows 环境变量不区分大小写，Foundation 读环境时建字典会撞重复键——
  **`Fatal error: Duplicate values for key`，进程直接没了**，不是能捕获的错误，
  而且崩在任何网络代码之前，报错里看不出跟代理有关。
- **`NO_PROXY` 必须设上**（本机环境里原本没有，只有 `HTTP_PROXY`）。不设的话发给
  `127.0.0.1` 的请求会被 v2rayN 接管，**连一个没人监听的端口也返回 503 而不是报错**——
  对离线优先的客户端这是要命的，它会把「网断了」读成「服务器出错了」。

**Swift 6 的严格并发默认开着**（`swift-tools-version: 6.0`）：跨并发域改全局变量直接编译不过。
对 Core 是好事，但要一开始就按它的规矩写，不能等写完再补。

## 部署

- 开发怎么跑：见「开发场地」。Mac 上直接 `uv run python run.py`
  （Windows 上那两个 `.bat` 还在，理由见 `run.py` 的模块注释）。
- **部署目标定为阿里云（x86_64，2G 内存），2026-09-14 定。** 这是终点，不是临时安排。
- **2026-09-20 起它就是唯一的服务端，不再是「开发期实例」。**
  用户当天定的「以后服务端就在阿里云，工程就在 Mac」。
  原先那句「正式迁移推迟到项目做完」已经兑现掉了——迁移就是这一次，没有下一次。
  **数据主副本从此在阿里云**（实测它是 r5s 的严格超集，见「开发场地」）。
- **改了服务端代码，手机那边不会自己变——阿里云跑的是镜像里的代码**（2026-09-16 栽的）。
  那天在本地改了三处服务端、跑了回归、全绿，然后让用户在手机上验——
  **而手机连的是阿里云，那台跑的还是前一天构建的镜像，一行新代码都没有**。
  于是「我已经会了」照旧不收队列行、拼写照旧重复提示、客户端发的
  `If-None-Match` 没人认。**客户端更新了不等于服务端更新了**，这两边的
  发版路径完全不同：App 走 TestFlight，服务端要 **git pull + 重新构建镜像 + 重启容器**。
  在阿里云上构建照 `/root/build-er.sh`（三条国内源已经写在里面）。
  **判断标准别靠记忆**：`docker exec english-reader grep -c <你刚加的函数名> /app/backend/...`，
  答案是 0 就是没上去。
  **同一天又兑现了一次**：改完写文章的提示词，不重建镜像的话阿里云那一轮会按旧提示词写，
  12 次尝试全被合格线拦下、出 0 篇。重建只花几十秒（代码层在 Dockerfile 最后，依赖层命中缓存）。
  **那台拉 GitHub 要挂代理**：直连 `GnuTLS recv error (-110)`，
  `https_proxy=http://127.0.0.1:7890 git pull` 就过。这跟「别给 Docker 守护进程配代理」不冲突——
  那条说的是构建时拉包。
- **夜间备稿跑在阿里云**（2026-09-16 起，因为 gemini 的 key 在那台）。
  从前 r5s 那份开关是故意关着的，怕两台各自 04:00 生成把 `content.db` 搞分叉；
  **2026-09-20 起只剩一台，那个顾虑消失了**。
- **数据只有一份，在阿里云。** 那条「只许 r5s → 阿里云 单向流动、永不回流」已经作废，
  连同「开发期实例上产生的标记、读完、复习记录全部作废」那句——
  **阿里云上那 27 条标记、230 条复习历史是真机上产生的真实记录**。
  （P10 会把学习记录整个清空，所以这批也留不住，但那是 P10 的决定，不是因为它们不算数。）
- **要在本地动数据，就得先让阿里云停手**：关掉夜间备稿、手机别用，
  把库拉下来（**用 `VACUUM INTO` 不要 `cp`**，有 WAL，直接拷可能不一致），
  跑完推回去、重建镜像、开回备稿。
  **老规矩没变：任何时候只有一份在被写。**
- 内存不是问题：后端常驻实测 152 MB（spaCy 占 134 MB），容器里 **117 MB**，那台 2G 绰绰有余。
- **形态：两个独立的 compose stack，靠一个共享网络 `proxy` 连起来**（2026-09-15 按用户惯例拆开）。
  `docker network create proxy` 每台机器建一次，两边都声明 `external: true`，谁先起都无所谓。
  - **本项目只剩 app 一个服务**，在仓库 `docker/` 下：`Dockerfile` + `docker-compose.yml` +
    `env.example`。**它一个端口都不发布**（`expose` 不是 `ports`），只能由反代按容器名访问。
  - **反代是宿主机的公共设施，不属于本项目**，在 `/opt/appdata/caddy/`（自己的 compose、
    `Dockerfile.caddy`、`Caddyfile`、证书目录）。**它不进版本控制**——将来别的服务也挂它，
    塞进这个仓库就等于让本项目拥有整台机器的入口。代价是重装服务器要重写那三十来行。
  - 域名、证书、公网端口全归反代管，**本项目的 `.env` 里不该再出现它们**。
- **数据库文件挂载到宿主机**，容器重建不丢数据。
- **三件动手前必须知道的事**（每一件都是实测撞出来的，不是预想的风险）：
  - **容器默认 UTC，而本项目读的是本地时钟**（`core/tasks.py:204`，`04:00` 是本地 4 点，
    复习「当天」也按本地零点切）。不设 `TZ` 就是夜间备稿在中午跑、日界提前八小时，**全程零报错**。
    镜像里已经写死 `TZ=Asia/Shanghai`。
  - **bind mount 的属主按 uid 数字比对，不按名字**。容器用户 uid 对不上宿主机目录，
    启动即 `PermissionError`。用 `ER_UID`/`ER_GID` 或把目录 chown 成 10001。
  - **不设 `ER_ADMIN_SECRET` 容器会崩**，而且报错跟密码毫无关系（它想往 root 拥有的
    `/app/.env` 追加生成的密码）。compose 里用 `${VAR:?}` 提前拦成一句人话。
- **那台上的数据来历**：2026-09-15 从 r5s 拷了一份快照过去（`VACUUM INTO`，三库 `integrity_check` 全 ok），
  此后它自己一路往前跑——**到 09-20 已经比 r5s 多 20 篇生成文、多一批真机记录**，
  于是顺理成章成了正本。`secrets.json` 后来补上了（只有 `gemini` 一个 key）。
  夜间备稿当初拷进去时是关着的，现在开着。
- **阿里云那台的现状**（2026-09-15）：`47.116.104.62`，Ubuntu 24.04 / x86_64 /
  **实际可用内存 1.6G，无 swap** / 磁盘 40G。`ssh aliyun` 可直连——
  **Mac 上的密钥是 `~/.ssh/id_rsa_aliyun`**（2026-09-19 配的，`~/.ssh/config` 里有 `Host aliyun`）。
  域名 **`reader.cntick.top`**。app 容器实测 healthy、时区 CST、四个库迁移完成。
- **备案拦截比我原先判断的更狠：高端口也挡，而且是按 SNI 掐 TLS**（2026-09-15 实测推翻前判）。
  原以为「未备案只挡 80/443，高端口可用」，实际是：`reader.cntick.top` 在 8443 上
  **先能用了四十分钟，随后被扫到并封掉**。
  **分清它的办法**（这是决定性的，别用别的）：对同一个 IP、同一个端口做两次 TLS 握手——
  **不带 SNI**，Caddy 会回一个 `internal_error` 告警（**有字节回来**）；
  **带那个域名的 SNI**，一个字节都没有。同时明文 HTTP 打同一端口拿得到 Caddy 的 400。
  端口是通的、Caddy 是好的、MTU 正常（1472 DF 通）——**只有带那个 SNI 的 TLS 被中途丢弃**。
  注意 `SNI=example.com` 这个对照**不成立**：Caddy 只配了一个站点，对陌生 SNI 本来就中断握手。
- **两条路都留着，2026-09-19 起 B 转正**（2026-09-15 定 A+B 并行，ICP 过审后按原计划切换）：
  - **A. Cloudflare Tunnel，现在是备用**：`https://reader.cfoptick.com`，
    `/opt/appdata/cloudflared/`。**服务器一个入站端口都不开**，没有 SNI 可供识别，拦不到。
    实测端到端 200，今日包 1 MB / 3 篇文章 / 21 条复习，**延迟 0.85–1.4 秒**——
    隧道连的是 Cloudflare 洛杉矶节点，流量绕太平洋一趟。容器没停，出问题随时切回来。
  - **B. Caddy 直连，2026-09-19 转正**：`https://reader.cntick.top`，`/opt/appdata/caddy/`。
    `.env` 的 `HTTPS_PORT` 8443→443、`HTTP_PORT` 127.0.0.1:8080→80，
    `docker compose up -d` 重建后验证：带 SNI 的握手拿到完整证书链（Let's Encrypt，
    `CN=reader.cntick.top`），`curl .../health` 跟完 307 拿到 200。
    证书到期仍是 2026-12-14。**App 里的服务器地址要手动从 cfoptick.com 换成 cntick.top**，
    这一步不随服务端改动自动生效。细节见 `deploy.html` §7。
- 证书**只能走 DNS-01**（未备案时 80/443 都不可达，HTTP-01 与 TLS-ALPN-01 都要那两个端口），
  而 DNS-01 要 Caddy 带 DNS 插件，官方镜像不带——`/opt/appdata/caddy/Dockerfile.caddy` 为此存在。
- **在阿里云那台上构建，三条取包的路都要换成国内的**（2026-09-15 实测，命令存在服务器
  `/root/build-er.sh`，重建照着跑）：PyPI 用 `PYPI_INDEX=https://mirrors.aliyun.com/pypi/simple/`、
  Go 模块用 `GOPROXY=https://goproxy.cn,direct`、spaCy 模型用 `SPACY_MODEL_URL` 指到
  `ghfast.top` 前缀。三个都是 Dockerfile 里的参数，**默认仍是官方源**，别改仓库里的默认值。
  - **别给那台的 Docker 守护进程配代理**：BuildKit 会把守护进程的代理注入每个构建步骤，
    而那台的 mihomo 只有 **73 KB/s**（实测 50 分钟拉了 220 MB），装依赖要几小时。
    去掉代理、改用镜像源之后，同样这一层 **496 秒**装完。
  - **`docker.io` 要配镜像源**。原有的 `docker.xuanyuan.me` 会 403 限流**且不回落到官方源**，
    整个构建当场失败。现配 `dockerproxy.net` 等三个，`python:3.12-slim` 17 秒拉完。
  - **宿主机连得上不等于容器里连得上**：`github.com` 在宿主机上 484 KB/s，
    **在容器里是 000（不通）**。判断某条路行不行，要在 `docker run` 里判，不要在宿主机上判。
- **HTTPS 是功能需求不是加固**：`client/App/project.yml` 的 ATS 只对局域网开了例外，
  **iOS 会直接拒绝连公网的明文 HTTP**。没有证书手机就连不上。
- **管理台一并暴露在公网，只靠 `ER_ADMIN_SECRET` 护着**（2026-09-14 用户定）。
  它后面是「下载整个学习库」那个按钮，所以密码要生成不要自己想。

## 沟通

- 用中文回复。

## 踩过的坑

统一写成「症状 → 原因 → 正确做法 → 现在谁守着」。**每一条都是真的栽过，不是假想的风险。**
上面那张触发表按「正要动手做什么」指到下面的小节——读到某一节就把那一节读完。

**记录规矩**：新坑往对应小节末尾加；坑被验收脚本守住之后，条目留着但在「守卫」里写上验收项号——
**守卫才是保护，这里的散文只是解释**。已经不成立的条目直接删，不留归档（归档是 `docs/` 那边的规矩）。

### 零、四条元教训

底下 27 条里反复出现的其实就这四个形状。**新坑八成还是它们。**

1. **已知答案的样本一直在手边，却没去对。** 同一个 `tags` 字段栽了四次（§5）。
   `labor` / `labour` 对一下就当场露馅，而它安静地过了整整一个阶段。
2. **让模型「找错」是生成任务，它会编造；让它判断具体对象是封闭任务，它很准。**（§1）
   开放式提问有产出压力，模型总能挤出点什么。
3. **一个测试在已知好的样本上报零，不等于它有效**——还得看它在已知坏的样本上报不报。（§4）
   阴性对照 + 阳性对照，缺一个都得不出结论。
4. **注释、方案、字段名说了什么，不等于代码做了什么。**（§7、§6）
   一段解释性的注释会让人以为对应的代码在那儿，读代码时就跳过它了。

---

### 一、改提示词前

#### 1.1 模型对提示词里的数量约束基本无感

**症状**：「不要超过五六处」无效（反而更高）、「至少用一两处」无效、义项集那次「合并成 3-5 项」也无效（给了 7 项）。
**原因**：数量是模型算不出来的东西，它只能大概比划。
**正确做法**：**说清楚「什么时候该用」**。被动语态就是靠给条件 + 写完自查，从 30.8 降到 15.5 的。
**2026-09-16 第四个实例，也是最值钱的一个**：用词上限写成「限制在最常见的约 4000 个英语单词以内」，
`gemini-3.8-flash-high` 十个种子**零合格**、超纲中位 5.82%。
换成**它认得的类别**——「四级大纲以内，六级／考研／雅思托福 GRE 才学的词一个都不要用」——
同样十个种子 **7/10、0.56%**，与 gpt-5.5 持平，命中率还升了。
**它不是不守规矩，是感觉不到 `mechanism`、`municipal`、`rigorous` 难**，直到你说这些词属于哪一级。
配套还有两段各自有效、合起来更有效的：**给一张越界词对照表**（16 个词各配一个替代说法）、
**要它写完逐词自查**。三段单独是 2.12 / 3.28 / 3.67，合起来 0.88。**互补，不是同一句话说三遍。**
**顺带一条反向的教训**：压住抽象词之后，剩下的越界词全变成 `storehouse`、`smokestack` 这类具体名词，
而「必须写具体的事」是另一条硬要求——所以那一段必须**给出口而不是下禁令**，
否则模型会靠把文章写空来守规矩。
**守卫**：无。改提示词时只能靠这条。数据在 `phase-4.html` §4 末节。

#### 1.2 一个错误的度量比没有度量更危险

**症状**：「形式主语几乎为零」，为此改了三轮提示词。
**原因**：校验器在数 `there be`——spaCy 只把 `there is/are` 标成 `expl`，`It is clear that…` 里的 `It` 是普通 `nsubj`。
改对之后真实值 8.91、基线 5.6，**一直是偏多而不是为零**。
**正确做法**：**改代码前先验证指标本身在数什么。**
**守卫**：无。见 §4.2 的通用做法。

#### 1.3 绝不让模型「找错」，只让它逐句做封闭判断

**症状**：问 `deepseek-v4-flash`「找出这篇文章里不地道的搭配」，它给**考试委员会审定的真题**判出均值 5.1 个「问题」
（`exerting influence on` 要改成 `having influence on`、`gap remains yawning` 要改成 `wide`）。
**原因**：开放式找错是生成任务，模型有产出压力。
**正确做法**：换成逐句的封闭判断——「这句话里的搭配，母语者会这么说吗？」
种下已知错误（真题句里 `make a decision` → `do a decision`）的对照测试里 **8/8 全中、零误报**。
**守卫**：无。这条是 B3「过量生成择优」的方法基础，写在主文档 §B3。

#### 1.4 同一条的第二个实例，比第一个更干净（2026-09-09）

**症状**：让 `deepseek-v4-flash` 通读一篇六级真题、「指出文章中所有的词组」，同一篇跑三次：
提出 37/28/34 个，**并集 50、交集 19，稳定率 38%**；只有 **21/50（42%）** 在 318,207 条词典里查得到；
**23 个在原文里根本找不到那个字符串**（`a you-and-me-against-the-world principle`、`strategic calculations`、`the cries of`）；
而且**连现有流水线已知的 13 个都漏了 3 个**。
**对照**：同一篇上，放宽后的结构筛提出 28 个、**全部在词典里、零编造**，其中 16 个是现有筛子漏的。
**正确做法**：**把候选锚在词典上**——词典里没有的组合它一个都提不出来，没有编造的余地。
**守卫**：无。数据在 `docs/phase-2.html` §18。

---

### 二、调模型 / 换模型前

#### 2.1 长文生成必须开流式

**症状**：写一篇 450 词的文章每次 524，而模型其实还在写。
**原因**：**网关掐的是沉默，不是长度**。gpt-5.5 要先思考约 109 秒才吐第一个可见 token，中转站在 120 秒掐断空闲连接。
**正确做法**：`llm_stream` 默认开（2026-09-09 加）。开了之后同一个请求 60 秒完成（453 词、超纲 0.85%、目标词 24/25）。
**关掉它长文生成直接不可用。**
**顺带**：中转站上非流式只有 `minimax-m2.5` 能写完整篇，但它目标词只命中 10/25、均句长 12.4；
其余模型把 2500 个输出 token 全烧在思维链上，正文 0 字。**能写完和守约束是两回事。**

#### 2.2 思考型模型会「成功地」返回空正文——流式也救不了（2026-09-10）

**症状**：`deepseek-v4.1-flash` 写文章四篇全空。流是通的、`finish_reason` 也有，就是没正文。
**原因**：`reasoning_tokens: 4000 / completion_tokens: 4000`，**delta 里只有 `reasoning_content`，`content` 一次都没出现**。
**正确做法**：三条解法——预算提到 16000（12.9s）、**`enable_thinking: false`（9.9s，最快）**、`thinking: {"type":"disabled"}`（13.1s）。
**排查手法记住**：模型「返回成功但没内容」时，**先把流里 delta 的字段名和 usage 打出来**——`reasoning_tokens` 会直接告诉你预算去哪了。

#### 2.3 别拿 star 数当权威依据

**症状**：`fsrs4anki` 有 4.1k star、`py-fsrs` 只有 481，差点据此选错。
**原因**：前者是**给 Anki 用的 JS 调度脚本 + notebook**（而且 FSRS 已内置进 Anki 23.10+，那个仓库对新版用户已无必要），
后者才是**能嵌进 Python 后端的库**。star 差的是用户群大小，不是权威性。
**正确做法**：**真正的权威证据是「FSRS 被 Anki 官方吸收成默认调度器」**，外加公开的 SRS benchmark 和十几种语言的独立实现。
**顺带**：许可要逐个核对。`py-fsrs` 是 MIT；**Anki 本身是 AGPL，绝对不能抄它的代码**；
同一个组织（open-spaced-repetition）下的仓库许可不一，有 AGPL 也有 CC0、BSD。

#### 2.4 别把「写文章」和「造例句」的横评结论搞混

**症状**：两次横评结论相反，容易记岔。
**原因**：任务大小不同——写 350 词整篇文章 vs 写一句话带一个指定的词。
**结论**：写文章 `gpt-5.5` 完胜（同一套提示词，超纲率 0.32% 对 `deepseek-v4-flash` 的 3.64%）；
**造例句不适用这条**（2026-09-09 横评 23 个模型）：合格率 92–100%，`deepseek-v4-flash-0731` 96% 反而高于 `gpt-5.5` 94%。
**顺带**：`deepseek-v4-flash` 那次横评 20 个模型的结论是**数据整理要的是不思考的模型**——
思考型模型把输出预算烧在思维链上，强模型反而在中转站的 180 秒网关超时里跑不完。

---

### 三、写管理页 JS 前

#### 3.1 `hidden` 在管理页里不一定隐藏得了

**症状**：设了 `el.hidden = true`，按钮**失效了但没消失**——「看得见却点不动」，代码全对而屏幕在骗人。
**原因**：`base.html` 给 `button` 设了 `display: inline-block`，优先级高过浏览器自带的 `[hidden] { display: none }`。
**正确做法**：已在 `base.html` 加 `[hidden] { display: none !important; }`。
**这类最难查，因为没有任何东西出错。**
**守卫**：`verify_phase0.py` 4.10。

#### 3.2 管理页继承 `base.html` 的全局作用域，别用它已有的名字

**症状**：**页面加载正常、之后每次点击都 404**，症状看着还是「按钮点不动」。
**原因**：复习页定义了同名的 `api()` 走 `/v1/client`，被 base 那个走 `/v1/admin` 的覆盖。
已被占用的名字：`api` / `toast` / `humanBytes`。P2 的阅读页把 helper 叫 `client()` 所以没事。
**正确做法**：**页面脚本一律包进 IIFE。**
**守卫**：`verify_phase0.py` 4.9 会扫描并报出撞车。

#### 3.3 Web 页的调试办法：抽出 `<script>`，在 node 里重放点击序列

**症状**：P3 人工验收时「按钮点不动」，猜了两轮都没找到。
**正确做法**：把 `<script>` 抽出来，配假 DOM 和假服务端，在 node 里重放点击序列。**重放一次就看清了**——
包括「答案面板没打开时点『对了』照样会提交」这种眼睛看不出来的。
**顺带**：**开发期的页面要 `Cache-Control: no-store`**——缓存的旧 JS 看起来和「没修好」一模一样。

---

### 四、加验收项或新指标前

#### 4.1 验收脚本不许依赖「今天碰巧有数据」

**症状**：`verify_phase3` 起初走当天真实的到期队列，于是复习做完之后再跑就一片红。
**原因**：**只在某些日子能过的验收脚本等于没有。**
**正确做法**：自己种探针、用完删掉，**前后各清一次**——中断的上一轮也不会污染下一轮。

#### 4.2 任何新指标上线前，先拿一个已知答案的样本验证它数得对

**症状**：难度画像里的「超纲率」量出来四级真题比考研还难。
**正确做法**：真题的三个等级就是现成的已知答案，任何难度类指标都该先在它上面跑一遍，看单调性对不对。
（这次是在写进代码之前跑数才发现的；§1.2 那次是改了三轮提示词之后。）

#### 4.3 阴性对照 + 阳性对照，缺一个都得不出结论

**症状**：搭配检查改好提示词之后真题和生成文都判成 ~0，看着像「文章没问题」。
**原因**：**分不清是「没问题」还是「裁判瞎了」。**
**正确做法**：补上**阳性对照**——拿真题句子把 `make a decision` 改成 `do a decision` 种个已知错误。
补上之后才知道模型是好的、问法是坏的。

#### 4.4 验收脚本会往真实告警通道里灌假警报（2026-09-11）

**症状**：用户刚打开 Bark，当天就收到两条失败推送——`任务失败：验收探针·失败`、`任务 probe.p4 的周期设置看不懂`。
**原因**：`verify_phase4` 有两项**故意制造错误**来验行为（任务抛异常、周期写坏），
`verify_phase0` 也每次报一条 `verify.failure` 验追溯链路。这些都是 ERROR，
而告警处理器挂在 logging 上，于是原样推给用户。
**为什么比噪音更糟**：`notifications.py` 的设计第一句就是「**一个狼来了的告警通道会被忽略**」。
验收每跑一次推两条假警报，就是在亲手训练用户忽略它——而它是夜间任务失败的唯一出口。
**正确做法**：`notifications.muted()`，验收脚本整段包进去。两处刻意设计：
① **是进程内变量，不是配置项**——用「跑前关掉、跑完打开」实现的话，脚本崩在中途就会
**把告警永久关掉**，而比误报更糟的失败模式是手机永远安静；
② **只静音推送，不静音日志**，ERROR 照旧全量落盘，查得到。
**守卫**：无。验的时候要用阴性+阳性对照（§4.3）——只看「没推」分不清是静音生效还是推送链路本来就坏了。

#### 4.4b 验收脚本会绊在自己刚制造出来的东西上（2026-09-12）

**症状**：`verify_phase2` 7.9「候选已逐处判断过」报「待判 5」。跑完之后再查，是 **0**。
**原因**：这个脚本自己会入库一篇文章，入库时结构预筛提出新的词组候选，**而判词组是异步任务**——
脚本在自己刚制造出来的候选还没判完时就去数了。
**正确做法**：**等，带上限**。改成轮询到 0 或者两分钟超时，超时了把「等了两分钟还没判完」写进说明——
那时它指向的是模型那条路断了，而不是代码坏了。
**这一条和 §4.6 是一家**：一个是脚本触发了真任务然后把它掐死，一个是脚本没等自己触发的任务。
**共同点是「脚本自己动了手，又假装只是在观察」。**

#### 4.4c 「待人工」的项数变多，可能只是今天没数据（2026-09-12）

**症状**：`verify_phase4` 从「38 通过、2 待人工」变成「34 通过、4 待人工」，看着像回归。
**原因**：C3.1 离线补报与 C4.1 幂等要「当天有未完成的复习条目」才跑得动，
而我刚用命令行客户端把当天的复习做完了。**脚本自己说了「当天没有未完成的复习条目，跑不了这一项」**。
**正确做法**：这两项该自己种探针、用完删掉（§4.1）。在那之前，**看到项数变化先读那一行的说明**，
它通常已经告诉你原因了——别急着当成回归去查代码。

#### 4.5 算法要用仿真验，单元检查查不出标定错

**症状**：FSRS 接线有两个错（把「一次过」当 Easy、没关掉自带 learning steps），在 1.1–1.6 全过的时候一直存在。
**原因**：**它们不让任何东西报错，只让间隔排得不对**——8→66→180 天，而正确的是 2→11→46→163→180。
**正确做法**：**让仿真里的学习者按 FSRS 自己的曲线遗忘**，消掉模型分歧，剩下的只有接线——
实测到期可提取性 0.901 对目标 0.900，偏离 0.0005。
**再换一个不同形状的遗忘模型，一次过率掉到 0.28**——那不是 bug，正是 optimizer 存在的理由。

#### 4.6 验收脚本会触发真实的定时任务，然后把它掐死在半路（2026-09-12）

**症状**：跑完一轮验收，`verify_phase4` 的 B5.1、B5.2 红了——「1 篇停在半路」「12 处待判词组」。
看着像刚改的代码碰坏了什么。
**原因**：验收脚本会 `create_app()`，于是**定时任务循环跟着起来**，夜间备稿正好到点触发，
真的生成了 3 篇、真的排了标注任务。**然后脚本跑完退出，线程池关掉**——
任务当场 `RuntimeError: cannot schedule new futures after interpreter shutdown`，
判词组那个则永远挂在 `running`。**花了钱，留下一篇打不开的文章，而验收把它报成自己的失败。**
**怎么认出来**：先看 `llm_jobs` 的 `error` 列——这一条的指纹就是 `after interpreter shutdown`。
**是环境不是代码**，形状同 §1.2：别拿着它去改根本没错的东西。
**收拾办法**（也是正常的运维动作）：重启服务让 `recover_interrupted()` 把僵住的 `running` 收成
`paused`——**但它有个 5 分钟的陈旧窗口**，刚死的任务收不到，等一会儿再重启；
然后控制台重启任务，查 `llm_jobs` 等它跑完（有超时上限）。
**没有守卫**。真正的解法是让验收脚本别把真任务跑起来，但那是要改验收框架的事。
在那之前：**跑验收之前先看一眼离 04:00 还有多久**，以及跑完检查一遍 `llm_jobs` 有没有留下 `running`。

---

### 五、查词典 / 判大纲前

**这个字段栽了四次。**四次的共同点是：**已知答案的样本一直在手边，却没去对。**

#### 5.1 第四次，也是最贵的一次（2026-09-09）

**症状**：阅读页把 `quickly` 标成超纲词，还说「不用学会」。
**原因**：「这个词在不在大纲里」有**三个调用方**——生成校验器算超纲率、入库打 `beyond` 标记、难度画像算超纲词比例。
**三方各写了一遍，只有生成校验器是对的**。它做了四件事，另外两方只做了第一件：

1. 词条自己的标签（并英美变体）
2. **表面形的标签**（`datum` 无标签，页面上是 `data`）
3. **词典自带的变形表**（`teeth` / `phenomena` / `curricula` 还原失败，但 ECDICT 的 `exchange` 认得）
4. **A 级派生的词根，且要顺链走到底**（`nationality → national → nation`，一步不够）

**实测**：453 篇里 8,401 处超纲标记，**3,005 处（35.8%）的词根就在纲内**——`quickly` 30 处、`fully` 28 处、`cultural` 27 处。修完降到 4,158 处。
**正确做法**：`vocabulary/syllabus.py` 一个函数，**三方都调它**，且**返回布尔不返回标签集合**——
返回集合就意味着每个调用方自己写比较，而它们正是各自停在了不同的地方。
B 级派生单独回答（`light_root`），它不算超纲但算三分之一个目标词，两个答案要能分开数。
**重算**走 `reading/recompute.py`（`POST /v1/admin/reading/recompute`），**只重写词典派生出来的值，
绝不碰 token 的身份、位置、义项标注**——那些是遇见记录，标记指着它们；前后各数一次行数，对不上就回滚。
**这次比前三次多学到一条：坏规则不只是让数字难看，它会让你选错。**
P3 的模型横评先用坏规则算，排出 gpt-5.5 92% > deepseek-v4-flash 88%；换成对的规则重算变成
**96% 对 94%，名次反了**——差一点就把活派给了贵四倍、慢四倍的模型。
**守卫**：`verify_phase2.py` 3.6（`quickly` / `teeth` / `nationality` 不许判超纲）与 **3.7（读源码断言三方调的是同一个函数）**。
**2026-09-18（P9）后果升级了一档**：大纲判定的结果现在随文章下发到设备，
而设备照着它画记号、决定哪些词能标。**判错一次不再只是这边数字难看，
是那个错误跟着今日包去了手机上**，而那台手机离线时连改都改不了——
只能等下一次下发。所以「三方调同一个函数」这条比以前更硬，不是更松。

#### 5.2 第三次：`tags` 在英式／美式拼写之间随机分配（2026-09-08）

**症状**：`labor` 被判成超纲——实测 21 个词条、125 处，`labor` 一个词 41 处。
**原因**：ECDICT 的 `tags` **不是各存一份**：`labour` 带 `zk gk cet4 cet6`，而 `labor` 只带 `ky`；
`judgement` 是四级词，`judgment` 一个标签都没有。
**正确做法**：`vocabulary/spelling.py` 和 `tags_of()` **就是为这件事写的**，模块开头第一段写的正是这个问题——调它。
（当时判超纲的两处 `ingest._is_beyond`、`difficulty.syllabus_tags` 没有调，安静地过了整整一个阶段。）
**守卫**：`verify_phase2.py` 3.5。

#### 5.3 第二次：`tags` 是分级清单，不是累积的（2026-09-07）

**症状**：超纲率 58%，四级真题量出来比考研还难。
**原因**：`the` 只带 `zk gk`（中考、高考），**并不带 `cet4`**；`make`、`people` 同理。
**正确做法**：「四级水平之内」= 带 `zk` / `gk` / `cet4` **任意一个**。改对之后超纲率 58% → 10%，三个等级也变成正确的单调上升。

#### 5.4 词形还原式和原样式各查一次

**症状**：漏义项报告里排第一的词是 `accord`，追下去是 **84 处 `according to` 一处都没识别出来**。
**原因**：spaCy 把 `according` 还原成 `accord`，而 `accord to` 在词典里没有条目。
**正确做法**：查词典时**两种形式各查一次**。
**顺带**：**报告本身就是诊断工具**——它排第一的那个词指的不是缺一条义项，而是识别少了一整类。

---

### 六、写数据库代码 / 解析别处写的数据前

#### 6.1 凡是解析别处写的数据，先看一眼它长什么样

**症状**：文章照样能读，**读完什么都不记**。不报错。
**原因**：`generation_drafts.target_words` 存的是 JSON 数组，按逗号空格切会切出 `["assignment"` 这种永远匹配不上的东西。
**正确做法**：**别照着字段名想当然**，先打印一条真实数据出来看。

#### 6.2 `sqlite3.IntegrityError` 被捕获后必须回滚

**症状**：几步之后一个**毫不相干**的请求报「database is locked」。
**原因**：失败的 INSERT 会把隐式事务挂着不放写锁。
**正确做法**：**吞掉数据库异常的地方都要问一句事务归谁收。**

---

#### 6.3 ISO 时间戳和 SQLite 的 `datetime()` 不能直接比（2026-09-11）

**症状**：`recover_interrupted()` 只恢复得了**昨天以前**中断的批次任务。当天崩溃的永远停在 `running`，
于是那篇文章的标注永远续不上，**文章永远停在 `annotating`、点开是空的，全程零日志**。
**原因**：存的是 `'2026-09-11T13:14:15+00:00'`（ISO，`T` 分隔），比的是 `datetime('now','-5 minutes')`
即 `'2026-09-11 13:09:15'`（空格分隔）。**`'T'` 的码位 0x54 大于空格 0x20**，日期段相同时后面一位就把结果定死——
同一天的比较永远为假，跨天才碰巧正确。
**正确做法**：**截止时间在 Python 里按同一种格式算出来再比**，别让两种格式在 SQL 里碰面。
**顺带**：这条最毒的地方是「跨天能用」——所以它能在测试里蒙混过去，只在当天重启时发作。

#### 6.4 「下次运行时间为空」＝这个任务永远不再跑（2026-09-11）

**症状**：夜间备稿任务二十七分钟一次没跑，`runs` 一直是 0，**没有任何日志说它为什么不跑**。
**原因**：`task_runs.next_due_at` 是 NULL，而判定是 `due <= now`——`None` 永远不满足。
播种逻辑又只在**行不存在**时才写，行在、值为空就原样返回。
**这个状态真实可达**：任务收尾时如果周期解析失败（在控制台把周期写错过一次），
`next_due_at` 就被写成 NULL，**从此永远不跑，把周期改回正确的也救不回来**。
**正确做法**：值为空要**重排并记 WARNING**，而不是当作「已排好」。
重排到**下一次**而不是「立刻」——修复动作不该顺手多跑一次生成。
**顺带**：这类「空值＝永远不满足条件」的判定，都要问一句**空是怎么来的、谁负责补上**。
**守卫**：`verify_phase4.py` A3.3。

#### 6.6 CI 的一步「成功」了也可能什么都没做（2026-09-12）

**症状**：L1「Core 能为 iOS 编译」这个 job 绿了，37 秒，**一行都没编译**。
**原因**：写成 `xcodebuild … | xcbeautify || xcodebuild …`，而**管道的退出码是最后一个命令的**——
xcodebuild 报错退出，xcbeautify 把输出消化完返回 0，于是这一步成功，`||` 的兜底也没触发。
底下真正的错是 `Supported platforms … is empty`：`xcodebuild -scheme` 要从 SwiftPM 包里推导
scheme 支持哪些平台，推导出来是空的。
**正确做法**：① **`set -euo pipefail`**；② 别用 `||` 兜底，它会把失败变成沉默；
③ **编完断言产物真的存在**——没有这一步，哪天又变成空跑，job 还是会绿。
**绿着的空跑比没有这个 job 更糟，因为它还给人信心。**
**顺带**：改用显式的 target triple 加 SDK（`swift build -Xswiftc -target …`），不让工具去推导。

#### 6.7 一个分不出两种情况的断言，等于没有断言（2026-09-12）

**症状**：为了防 6.6，我加了「确认产物是 arm64」的断言——它照样会在「iOS 目标被忽略、
实际编成了 macOS」时通过，因为**两个平台的产物都是 arm64**。
**而且构建目录名写着 `arm64-apple-macosx`**（SwiftPM 按宿主命名），看上去更像是编错了。
**正确做法**：读 Mach-O 的 load command——`otool -l x.o | grep -A5 LC_BUILD_VERSION`，
`platform 2` 才是 iOS。**读不出来要报红，不能默默放过**：一个说不清自己验了什么的检查，
比没有更糟。
**这条和 §4.3 是同一个形状**：不是「有没有报错」，是「它区分得开吗」。

#### 6.5 任务「成功」了也可能什么都没做（2026-09-11）

**症状**：标注任务显示 6/6 全部完成、耗时 0.03 秒、零错误，而 275 个实词一个都没标注。
**原因**：任务规划时把 token id 存进了 payload，之后文章被重新分析过一次，**token 被删了重建、id 全变**。
每一批都按旧 id 去查，查出来是空的，于是「没有需要处理的内容」——**报成功**。
**正确做法**：**按业务对象自愈，不要按任务自愈**。要问的不是「任务跑完没有」而是
「**文章现在能不能打开**」——把所有「备好但没就绪」的文章按当下真实缺的 token 重新排上。
**守卫**：`verify_phase4.py` B5.1（不许有备好却打不开的文章）。
**2026-09-18（P9）这个坑的后果变了，而且变得看不见了**：token 被删了重建、id 全变，
以前只伤服务端自己（文章打不开，一眼看得出来）。**记录搬到设备之后，标记指着的正是这些 id**——
id 一变就是**别人手机上的标记静默失效**，而这边一切正常。
所以「内容 id 不可变」从实现细节升成了架构铁律 5：**重新分析必须是加一个新版本，不是原地重建。**

### 七、读代码时、等后台任务时

#### 7.1 一段解释性的注释会让人以为对应的代码在那儿

**症状**：404 带着 trace_id 发给用户、什么也不写；用户把 trace 贴过来，**查出来是空的**。
而「贴 trace 就能定位」正是把 trace_id 放进响应的全部理由。
**原因**：`core/errors.py` 的 404 处理器上写着「保持 INFO 级：拼错地址不算事故」，**下面没有任何一行 log 调用**。
**正确做法**：读代码时**不要把注释当成实现的描述**——它描述的是意图，实现可能从来没跟上。
**守卫**：`verify_phase0.py` 9.4。

#### 7.3 查残留进程要查「自己丢到后台的那些」，不只是服务（2026-09-12）

**症状**：P5 全部做完、收尾查过好几遍「没有残留进程」之后，**用户看到后台还有一个 shell 在跑**。
是一条 `find / -name "Package.swift" -path "*swift-openapi-generator*"`——
Git Bash 里的 `/` 会扫遍 C: 和 D:（D: 上 516 GB），**跑了三个半小时、烧掉 13000 秒 CPU**。
**原因有两层**：① 那条命令本身就不该写，我几秒后就用别的办法查到了答案，扫描却没停；
② 收尾时我每次只查 `python|swift|uvicorn` 和端口占用，**从没查过自己超时丢到后台的一次性命令**。
**正确做法**：**别在 Windows 上写 `find /`**，要找第三方包就去它该在的目录找。
收尾那一查要按 CPU 排，不要按进程名过滤——名字是猜的，CPU 不是：
`Get-Process | Where-Object { $_.CPU -gt 60 }`。
**这条最扎眼的地方**：CLAUDE.md 早就写着「收尾时顺手查一次，**不要留给用户去发现**」，
而这次正好是用户发现的。

#### 7.7 **「我修好了一个真的缺陷」不等于「我修好了他报的那个问题」**（2026-09-19）

**症状**：用户报「切选项卡很卡，还在转圈」。我从日志里挖出四个缺陷，
每一个都确凿（服务端 trace 里的 422、成对出现的日志行、存档里的越界值），
**每一个都修了、都配了回归测试**——推了三版（11／13／14），
而用户那边**症状一次都没变**。
**原因**：我一直在回答「日志里有什么不对」，而他问的是「屏幕为什么在转」。
那是两个问题。**事后的日志能证明某个请求失败了，证明不了某一帧为什么没画出来**——
主线程花了多少毫秒、切屏时重建了哪些视图、`.task` 被取消了几次，日志里一个字都没有。
**正确做法**：**症状类的报告，先想清楚「我要看的那个东西，现在的手段看得见吗」**。
看不见就先解决看得见的问题（这次的答案是换场地到 Mac），
**而不是拿看得见的那部分连猜三轮**——每一轮都消耗一次用户的真机验收，
而那是这个项目最贵的资源（P6 §13 就是为它欠了三个 Phase 的账）。
**顺带一条**：修掉的东西要如实说成「修了一个真的缺陷」，不能说成「应该好了」。
我三次都说成了后者。
**守卫**：无，这类守不住。`phase-9.html` §17 把这次的账算清了。

#### 7.5 **`@MainActor` 不防重入**，而「每一行日志都是双份」是认出它的办法（2026-09-18）

**症状**：手机「一直转圈而且非常卡」。日志里 `adopted=399` 出现两次、
`progress.pool.reported` 出现两次、`pending` 从 0 跳到 798（正好 399×2）。
**原因**：`AppModel` 是 `@MainActor`，我据此以为 `drain()` 不会并发——
**而 MainActor 只保证「都在主线程上跑」，不保证不重入**：`await` 一让出去，
第二个调用就从头跑起来。启动时 `.task` 和 `scenePhase == .active` 几乎同时到。
两趟 `pull()` 各自在开头取一份「已知幂等键」快照，于是同一批事件进日志两遍，
而两边的游标写入互相覆盖。
**正确做法**：要互斥就自己写互斥（一个 in-flight `Task`，并发进来的**等它**
而不是丢掉——`await drain()` 的含义是「一趟同步完成了」）。
**怎么认出来**：**日志里每一行都是双份**。这比任何推理都快，
而且它在 09-16 的日志里就已经是双份了——那时不致命，所以没人看它。
**守卫**：无。回归测试在 `SyncTests`（分片那条）与 `EventLogTests`（幂等那条）。

#### 7.6 同一个方法、同一个形状、隔两天栽两次（2026-09-18）

**症状**：2026-09-16 真机上「越用越慢」，答得越多越卡。
**原因**：`refreshPendingCount()` 里塞了 `outbox.pending()`，而那个方法会
**读取并解码发件箱里的每一个文件**；`record()` 每记一条事件就调一次它。
**2026-09-18 我在同一个方法里塞了 `events.load()`**（为了显示「日志一共多少条」），
形状一模一样，原因完全不同。
**正确做法**：贵的计数按需算（`refreshEventCount()` / `refreshDamagedCount()`），
**而禁令写在那个方法的注释里**，不只记在这份坑里——
第二次栽的时候我正在读这份文件的别处，而注释就在我手指下面。
**守卫**：无。

#### 7.4 `pkill -f <模式>` 会杀掉你自己那条命令（2026-09-15，一小时里栽了三次）

**症状**：通过 ssh 在远端跑 `pkill -f "docker compose --env-file"`，命令**没有任何输出就结束了**，
后面的步骤一步都没执行。看起来像 ssh 断了，其实是**它把自己杀了**。
**原因**：`pkill -f` 匹配的是进程的完整命令行，而**我那条 ssh 命令的命令行里就含着那个模式串**——
`ssh host 'pkill -f "X"'` 的远端 shell 命令行里有 `X`，于是它匹配到自己。
写成脚本也救不了：只要 ssh 命令行里出现过那个字面量（比如用 heredoc 把脚本内容传过去），
远端那条 `bash -c` 的命令行里就有它。**第三次是在 `pgrep -f "bake"` 上栽的**，
当时我以为换成 pgrep 就没事了。
**正确做法**：**先用一次调用取 PID，再用下一次调用按 PID 杀**。
非要在一条命令里做，就让模式串在命令行里长得不一样：`pgrep -f bak"e"`——
pgrep 收到的是 `bake`，而我的命令行里是 `bak"e"`，匹配不上。
**怎么认出来**：远端命令**静默地少执行了后半截**，而且没有任何错误。
和 §6.6 是一家——**失败被变成了沉默**。
**守卫**：无。

#### 7.2 等待条件不要依赖一个后来又被自己改变的东西

**症状**：循环永远不退出，shell 一直空转，控制台一直显示「还有任务在跑」，让人以为有事没做完。
**原因**：犯过两次同一类错误——第一次是等一个永远凑不满的输出行数（自己用 `tail` 截掉了前几行），
第二次是等一个日志文件里的完成标记（自己在收尾时 `rm` 掉了那个文件）。
**正确做法**：见上面「后台任务与等待」那四条规则（查数据库不轮询日志、循环必须有超时、清理放最后、收尾查残留进程）。

**2026-09-15 第三次，形状又变了一点**：等的是**一个已经死掉的进程的输出**。
两个 `until grep -q ...; do sleep; done` 分别在等两个探针脚本写出结果，
而那两个脚本一个被 `timeout` 杀了、一个 import 失败当场退出——
**它们的输出永远不会出现，于是循环永远不退**。
最后是机器内存不足把它们杀掉才结束的（当时在 r5s 上，3.6G 还跑着九个容器）。
**「循环必须有超时上限」这条就是为这个写的，而我没照做**——
等待条件不只要防「自己后来改掉它」，还要防「产生它的那个进程已经不在了」。
**顺带**：别并行跑验收脚本，每个都要加载 spaCy（134 MB）加一整个后端——
在小内存机器上这会直接被 OOM 杀掉（r5s 上栽过），Mac 上不至于，但一样是白烧。

---

### 八、跨 Phase 的静默错误清单（只有一行，但都真的发生过）

- **入库绝不写 `study_states`**：破了它，没读过的文章里的词会被静默记为已学、从此不再出现——没有报错、没有日志。守卫 `verify_phase2.py` 7.1。
- **撤销标记后要退回 `new`**，但**遇见记录保留**（它确实被遇见过）。守卫 7.13。
- **换义项集时不要重新标注**：`store_senses` 是删了重插，义项 id 全变，一次全量替换会静默作废 95,058 条标注（460 万输入 token）和全部考频。做义项级映射，约 7,200 次调用。
- **词组只高亮半个词**：第一版点任一半都开面板，但高亮只亮半个词、标记后正文毫无反应。**高亮半个词，等于用更安静的方式说了同一句错话。**
- **方案写了但代码没写**：「读完文章自动补句子池」先前写进了方案没写进代码，拨到第二天才发现「抽到的题没有句子」。守卫 `verify_phase3.py` 5.5。
- **验收脚本会到期**：`verify_phase2.py` 7.5 原本断言「调度字段不存在」，P3 做了之后这条到期。改成守始终成立的那一半（入库与阅读都不许碰调度字段）。**删掉会丢守卫，留着会天天报假警。**
- **验收脚本把版本号写死，改版之后它只看得见旧数据**：`verify_phase1c.py` 7.1 查的是 `prompt_version = 'p1b-1'`。提示词一升版（2026-09-16 升到 `p1b-2`），这条**照旧通过，量的却是上一版的成绩**，而且没有任何东西会提醒你。改成先问当前版本、没有产出时回落到最近有数据的那一版，**并把用的是哪一版印出来**——回落是合理的，隐瞒不是。**凡是按版本号、日期、模型名取样的断言都是这个形状。**
