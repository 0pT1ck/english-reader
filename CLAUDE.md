# English Reader

面向 CET-4 / CET-6 的英语阅读学习 App。核心机制是从阅读中习得词汇，并用真实语境安排复习。

## 文档

**为方便用户读**而做成 HTML（`docs/`，浏览器直接打开，**不要发布为 Artifact**，样式全在文件内不依赖网络）。
**AI 一样要读**——功能设计、每个 Phase 的决定与依据全在里面，这份 CLAUDE.md 只给指针：

- `docs/english-reader.html` — 功能设计（做什么、为什么这么做、开发路线、架构前提）
- `docs/phase-0.html` — P0 方案（八项技术定案、要建的东西、明确不做的边界、验收清单）
- `docs/phase-1.html` — P1 方案（三段推进、已定决策、P1a 工作流、验收标准）
- `docs/phase-1b.html` — P1b 方案（API 集成、词典修复、义项集、词族、生成改进）
- `docs/phase-1c.html` — P1c 方案（用 Wiktionary 清单 + 真题考频重建义项集）
- `docs/phase-2.html` — P2 方案与实施结果（§1–§12 阅读闭环、§13 P2y 词组、§18 词组召回缺口）
- `docs/phase-3.html` — P3 方案与实施结果（§2 二十八条决定、§11 模型横评、§12 实施结果）
- `docs/archived.html` — 归档（从主文档移出的内容，原文照录，附移出理由）。
  **不需要读它**——里面全是已经不成立的东西，读了只会把废弃口径当成现行口径。
  它只是留档备查，用户想翻旧账时自己打开；只有用户明确指名要看某条归档内容时才去读。

**这份 CLAUDE.md 每次会话整份进上下文**，所以它只放「随时可能改变下一步动作」的东西：
铁律、跨 Phase 不变量、现行配置，外加末尾那份**踩过的坑**——
那份曾经拆成单独文件，2026-09-10 又并了回来，理由见该节开头。

**文档组织规范**：功能设计集中在主文档，每个 Phase 的实施方案各自一份 `docs/phase-N.html`。
讨论产出的结论更新进对应文档，不要随意另开新文档。**实施细节写进 phase 文档，不要抄进这里**——
这份文件每次会话整份进上下文，抄一遍就是每次都付一遍钱，而且两份必然漂移。

**主文档只描述现在的系统。** 已经不成立的决定、被推翻的数字、归档的旧路线，全部移进
`docs/archived.html`，并在原位留一句指路——**移出不等于作废**，原文与实测数据照录、附移出理由。
「不等于作废」是对历史而言：**对当前开发它等同于不存在，不要去读**，主文档那句指路就是全部所需。
2026-09-10 第一次这么清的：每日配额与周期预测、「句法复杂度始终对齐 CET 真题」这句保证
（**这等于在密度与句法的取舍里选了密度**）、设计前提 03「单用户系统」（它与 P2 契约留的身份口子说反话）、
按分钟数定的时间限定，以及早已标着归档却还占着 §K 半张表的原路线 P3–P8。

## 动手前先看末尾「踩过的坑」的对应小节

**这七种情形每一种都栽过至少一次**，其中「查词典 / 判大纲」栽了四次。

| 我正要…… | 读 |
|---|---|
| 改提示词 | §1 数量约束无感 · 错误的度量 · 绝不让模型「找错」 |
| 调模型 / 换模型 | §2 长文必开流式 · 思考型模型返回空正文 · 别拿 star 数当依据 |
| 写管理页 JS | §3 `hidden` 隐藏不掉 · 全局作用域撞名 · 用假 DOM 重放点击 |
| 加验收项或新指标 | §4 不许依赖「今天碰巧有数据」· 阴性+阳性对照 · 算法要用仿真验 |
| 查词典 / 判大纲 | §5 **同一个 `tags` 字段栽了四次** |
| 写数据库代码 / 解析别处写的数据 | §6 先看一眼数据长什么样 · `IntegrityError` 必须回滚 |
| 读代码、等后台任务 | §7 注释描述的是意图不是实现 · 等待条件别依赖自己会改的东西 |

## 仓库

https://github.com/SCmenghua/english-reader （私有，默认分支 `main`）

代码进版本控制，数据不进：`data/` 下的数据库、词典源文件、`.env`（含管理密码）、
`data/secrets.json`（API 密钥）全部在 `.gitignore` 里。备份走管理控制台的下载按钮，不走 git。

数据库按「丢了会怎样」分两类：**必须备份**——`learning.db`（学习记录）与 `content.db`（花钱生成的
义项集、例句、词族）；**不用备份**——`dictionary.db`（可从 ECDICT 重建）与 `logs.db`（可丢弃）。

## 进度

按 Phase 推进，路线见主文档 §K。**只规划当前 Phase，不提前规划未来**（2026-09-09 起）——
§K 表里只有「已完成的」和「当前这个」，发现的未来需求记进主文档 §M 待定，不动手。

| Phase | 状态 | 细节 |
|---|---|---|
| P0 地基 | 2026-09-05 验收 | `verify_phase0.py` 32 项 |
| P1 / P1a | 2026-09-05 验收 | 「搭配无误」这条完成定义**未独立验证**（盲测已取消，只有本人试读），别当成验证过的事 |
| P1b / P1c | 2026-09-06 验收并提交 | 28 项 / 16 项。义项集重建搁置，走的老办法补全，理由见 `phase-1c.html` §11 |
| P2 阅读闭环（含 P2y 词组） | 2026-09-08 验收并提交 | 自动 47 项、人工 6 项。`phase-2.html` |
| **P3 复习** | **代码完成，自动 42 项全过；人工三项推迟** | `phase-3.html`，`verify_phase3.py` |

**P3 人工三项推迟到 P7 iOS 端之后再验**（2026-09-10 定，见 `phase-3.html` §10 末尾与主文档 §M）——
三项验的都是体感，而现在唯一能跑复习的是电脑上的管理页，**按架构铁律 8 它是开发期测试工具**，
在它上面坐三到五天量出来的是「凑合跑」的体感。**两条代价要认**：铁律「验收通过才进下一个」被绕过，
P3 会以未验收状态带着走好几个 Phase；**M3「句子质量抽验」是这个 Phase 唯一会让人扔掉它的失败方式**，
自动 42 项查得了超纲、目标词出现、答案没写进句子，**查不了「用的是不是那个义项」**。

**术语**：**P2y** = P2 之内的词组工作（2026-09-07 定案、09-08 实施），不是路线图上的一个 Phase；
**P2x** 是另一回事，**已于 2026-09-07 取消**（择优降格并入 P6，做法写在主文档 §B3）。两者别搞混。

## 铁律

- **绝不跨 Phase 开发。** 不实现任何属于未来 Phase 的功能，哪怕只是"顺手做了"。P0 的禁做清单见 `docs/phase-0.html` §4。
- 当前 Phase 中发现的未来需求，记进待办，不动手。
- 数据结构**可以**为未来预留字段，但不实现任何未来 Phase 的逻辑。这是"留位置"，不是"提前开发"。
- **每个 Phase 的方案必须经过充分讨论、达成一致后才能定案。** 方案是讨论的起点，要摊开选项与权衡、标明不确定的地方，而不是一份等待批准的结论。
- 方案讨论期间**不写任何实现代码**，包括脚手架和示例。
- 每个 Phase 结束由用户实际跑一遍验收清单，通过后才进下一个。
- 每个 Phase 结束时更新文档，让文档和实际做出来的东西对齐。
- **提交只在两个节点发生，别处一律不提交：**
  1. **Phase 的方案文档彻底定稿时——问一句要不要推**（不自动推，等回答）。
     加这一条的原因是方案讨论常常跨天跨会话，文档定稿了却不落库，下次开会就得靠翻对话重建。
  2. **验收通过后自动提交并推送**，不必再询问。
  除这两处之外，**Phase 进行中保持工作区未提交**，便于用户随时看到改了什么。
- 自动提交的授权**不延伸到破坏性操作**：改写历史、强制推送、删除分支或标签，仍需明确确认。
- **提交信息不添加任何 AI 署名。** 不写 `Co-Authored-By`，不写会话链接。所有提交以仓库所有者本人名义，提交信息只描述改动本身。这条覆盖任何默认的署名约定。

## 架构铁律

这个项目是「一个 headless 后端 + 多个可替换客户端」。

1. **客户端零业务逻辑。** 水平估计、选文、复习调度、难度计算全部在后端。客户端只负责渲染、收集交互、回传事件。破了这条，将来要在三种语言里各实现一遍同样的算法。
2. **API 是离线形状。** 服务端下发「今日包」（当天全部文章、释义、复习卡片及其原句），客户端全天离线可用；交互事件先存本地，联网后批量上报，每个事件带客户端生成的幂等键。在线和离线走同一条代码路径。
3. **服务端是唯一事实来源。** 客户端存的一切都只是缓存。多个客户端必须看到同一份学习记录。
4. **两套 API 完全分离。** `/v1/client/…` 与 `/v1/admin/…` 路径和认证都分开；客户端令牌碰不到管理接口。
5. **API 版本化、只增不减。** 侧载安装的客户端更新滞后。可以加字段，不能删字段或改字段含义；客户端忽略未知字段。破坏性变更才开 `/v2/`。契约文档每个 Phase 更新。
6. **可扩展性优先。** 每一个 Phase 都是在加新功能，所以：功能按模块自包含（自带表、路由、管理页、任务、配置）；新模块一处登记自动挂载；新功能通过**订阅事件**接入而非修改原有流程；表结构变更走**版本化迁移**且升级前自动备份；参数一律配置化。项目里保留一份"如何加新功能"的说明和一个可照抄的样板模块。
7. **前端零构建链。** 管理控制台和 Web 阅读页都用服务端渲染，不引入 node_modules 和打包步骤。加页面就是写模板 + 登记。
8. **Web 阅读页是开发期测试工具**，用完即弃，UI 力气留给 P7 的 iOS 原生端。**Web 管理控制台长期存在**，要好用、稳定、信息全——它是诊断通道和备份入口。

## 跨 Phase 不变量

改动时别破。破了这些的后果都是**静默的**——没有报错、没有日志，末尾「踩过的坑」§8 有一份症状清单。

- **记账只在读完那一刻发生，入库绝不写 `study_states`。** 守卫 `verify_phase2.py` 7.1。
- **进复习队列只有两条路，都是你自己的信号**：你标记了，或读后抽查答错了（D3）。
  推论：**超纲词也能标、也能进队列**——「不在大纲里」是关于大纲的事实，「不许学」是替用户做主。
  不计生词率照旧（那个数描述的是文章对大纲，不是文章对你）。
  **读完一篇文章只增加遇见次数，不改变词池位置**，不管是真题还是为教那些词而写的生成文。
  试过两版自动记账，都推翻了。守卫 7.4。
- **撤销标记后条目退回 `new`，但遇见记录保留**（它确实被遇见过）。守卫 7.13。
- **词组是一个整体，就按一个整体渲染**——这条对所有客户端都成立，P7 也算。
  静止状态正文里不加记号（下划线三种、底色两种已占满），但**悬停、点选、标记都整体发生**，
  一处词组渲染成一个元素，两个词连同中间的空格都在里面。静止要不要标出来是各客户端自己的呈现决定，
  「选中与标记按整体」不是。
- **契约里给未来留位置，值先为空**：水平估计、义项的记忆状态、专有名词说明这类将来某个 Phase 才填的东西，
  字段在契约里就要有位置，并由响应顶层的 `capabilities` 声明区分「没数据」和「没实现」。
  但**只留形状显而易见的位置**（布尔、一句话、一个数、可整体为 null 的对象）；
  复习卡片、抽查题、今日包这类结构复杂的不猜，将来以**新增端点**接入。详见 `phase-2.html` §6。
  身份也是这么留的：由设备令牌推导，响应顶层回显 `learner`，路径里不出现人的编号。
- **换义项集时不要重新标注。** `store_senses` 是删了重插、义项 id 全变，一次全量替换会静默作废
  95,058 条语境标注（460 万输入 token）和全部真题考频。正确做法是**保留旧表、做义项级映射**（约 7,200 次调用）。
  `senses.replaced` 事件是给**单词补全**用的，不是给换词典用的，别搞混。
- **`is_exam_key` 熟词僻义标记保持为 0。** 规则本身成立，但它依赖可信的义项排序，而现在的序号是模型猜的
  （按它会把 `well`「好」、`even`「甚至」标成僻义，占全部义项的 19%）。理由见主文档 §F4。
- **不要问模型「哪些义项值得学」**——它会答最常见的，而四六级考的是熟词僻义。
  只能问「这个义项属不属于通用书面英语」。

## 模型与中转站

- **分工别搞反**：数据整理用 `deepseek-v4-flash`（快、便宜、准），**写文章用 `gpt-5.5`**——
  同一套提示词超纲率 0.32% 对 3.64%。**但「给一个义项造例句」不适用这条**，见「踩过的坑」§2.4。
- **长文生成必须开流式**（`llm_stream`，默认开）。关掉它长文生成直接不可用，原因见「踩过的坑」§2.1。
- **中转站限速 20 rpm**，这条比模型延迟更决定批次总耗时。
  `seed-2.1-*` 与 `qwen3.8-max` 全部 504，`minimax-m2.7` 全部 429。
- 生成用哪个提供商由配置项 `gen_provider` 指定，**不走「默认提供商」**。
  `shuai` 提供商额度有限，**当前已停用**，需要时在控制台启用。
- `deepseek-v4.1-flash` 实测比现用的都强但**还换不了**（正式版名在中转站是 `MODEL_DISABLED`），记在主文档 §M。

## 日志规范

日志的设计目标是：用户说"不对劲"时，AI 能查出来到底怎么了。

- **三类分开**：技术日志入 `logs.db`（30 天轮转，可丢弃）；决策日志入 `learning.db`（长期保存，记录每次判断的输入、候选、理由）；业务记录本身就在业务表里，不重复记。
- **事件名是固定标识符**，不是自由文本（`article.generation.failed`，不是"生成失败了"）。这样才能精确过滤和统计。
- **trace_id 贯穿**一次请求或任务，并出现在错误响应里——用户复制给 AI 就能定位完整链路。
- **DEBUG 平时只在内存滚动**，发生 ERROR 时自动落盘该 trace 的详细记录。
- **语言**：事件名和字段名英文，说明文字中文，原始报错原样保留。不做"人话翻译层"——用户有疑问直接问 AI。
- **通知**：Bark，仅推生成失败 / 服务异常 / CRITICAL，带去重。
- **API 密钥一律不入日志。**

## 代码规范

- 注释用**英文**，与标识符同语言。项目特有概念在模块头部给中英对照（如 `target word 目标词` / `incidental word 附带词`），以便和中文设计文档对上。
- 注释解释**为什么**这么做，而不是复述代码在做什么。用户不读代码，注释是写给未来接手的 AI 看的。
  但**注释描述的是意图，不保证实现跟上了**——读代码时别拿它当实现的描述，见「踩过的坑」§7.1。

## 后台任务与等待

批量任务动辄跑几十分钟，必须放后台。等待它的方式犯过两次同一类错误，见「踩过的坑」§7.2。

- **查状态优先查数据库，不要轮询日志文件。** 批次任务表里就有 `status` 和 `done/total`
  （`llm_jobs`），那是权威来源，也不会被清理脚本删掉。
- **等待循环必须有超时上限**，超时就退出并说明情况。绝不写没有出口的 `until … do sleep`。
- **清理放在最后一步。** 还有东西在监视某个文件时，不要删它。
- **任务结束后确认没有残留进程**——收尾时顺手查一次，不要留给用户去发现。

## 文件位置

- **所有生成的文件都放在项目目录 `D:\code\English Reader` 下**，不要写进 scratchpad 或系统临时目录。文档放 `docs/`。
- 数据库文件放 `data/`，**不进版本控制**（体积 + 隐私）。词典源数据同样不进仓库，用脚本下载。
- 只有真正一次性的中间产物才用临时目录。

## 网络访问

本机通过 v2rayN 出网，混合端口 **10808**（同时支持 http 和 socks5）。任何需要访问外网的命令、脚本或代码都要走它。

- 代理地址：`http://127.0.0.1:10808`，或 `socks5://127.0.0.1:10808`
- 环境变量：`HTTP_PROXY` / `HTTPS_PROXY` / `ALL_PROXY` 设为上述地址；`NO_PROXY` 设为 `localhost,127.0.0.1,::1`
- 应用代码里的出站请求（LLM API、Bark 推送）走配置里的代理地址，不写死

## 部署

- 开发：Windows 本机直接跑 Python，热重载。服务用 `start.bat` 启动，开发用 `dev.bat`。
- 部署：Docker 多架构镜像 → NanoPi R5S（ARM64，4GB 内存，1TB SSD），未来可能迁云。
  **按决定推迟到整个项目开发完毕**，开发全程在 Windows 本机进行。
- **数据库文件挂载到宿主机**，容器重建不丢数据

## 沟通

- 用中文回复。

## 踩过的坑

统一写成「症状 → 原因 → 正确做法 → 现在谁守着」。**每一条都是真的栽过，不是假想的风险。**
上面那张触发表按「正要动手做什么」指到下面的小节——读到某一节就把那一节读完。

**记录规矩**：新坑往对应小节末尾加；坑被验收脚本守住之后，条目留着但在「守卫」里写上验收项号——
**守卫才是保护，这里的散文只是解释**。已经不成立的条目直接删，不留归档（归档是 `docs/` 那边的规矩）。

### 零、四条元教训

底下 18 条里反复出现的其实就这四个形状。**新坑八成还是它们。**

1. **已知答案的样本一直在手边，却没去对。** 同一个 `tags` 字段栽了四次（§5）。
   `labor` / `labour` 对一下就当场露馅，而它安静地过了整整一个阶段。
2. **让模型「找错」是生成任务，它会编造；让它判断具体对象是封闭任务，它很准。**（§1）
   开放式提问有产出压力，模型总能挤出点什么。
3. **一个测试在已知好的样本上报零，不等于它有效**——还得看它在已知坏的样本上报不报。（§4）
   阴性对照 + 阳性对照，缺一个都得不出结论。
4. **注释、方案、字段名说了什么，不等于代码做了什么。**（§7、§6）
   一段解释性的注释会让人以为对应的代码在那儿，读代码时就跳过它了。

---

### 一、改提示词前

#### 1.1 模型对提示词里的数量约束基本无感

**症状**：「不要超过五六处」无效（反而更高）、「至少用一两处」无效、义项集那次「合并成 3-5 项」也无效（给了 7 项）。
**原因**：数量是模型算不出来的东西，它只能大概比划。
**正确做法**：**说清楚「什么时候该用」**。被动语态就是靠给条件 + 写完自查，从 30.8 降到 15.5 的。
**守卫**：无。改提示词时只能靠这条。

#### 1.2 一个错误的度量比没有度量更危险

**症状**：「形式主语几乎为零」，为此改了三轮提示词。
**原因**：校验器在数 `there be`——spaCy 只把 `there is/are` 标成 `expl`，`It is clear that…` 里的 `It` 是普通 `nsubj`。
改对之后真实值 8.91、基线 5.6，**一直是偏多而不是为零**。
**正确做法**：**改代码前先验证指标本身在数什么。**
**守卫**：无。见 §4.2 的通用做法。

#### 1.3 绝不让模型「找错」，只让它逐句做封闭判断

**症状**：问 `deepseek-v4-flash`「找出这篇文章里不地道的搭配」，它给**考试委员会审定的真题**判出均值 5.1 个「问题」
（`exerting influence on` 要改成 `having influence on`、`gap remains yawning` 要改成 `wide`）。
**原因**：开放式找错是生成任务，模型有产出压力。
**正确做法**：换成逐句的封闭判断——「这句话里的搭配，母语者会这么说吗？」
种下已知错误（真题句里 `make a decision` → `do a decision`）的对照测试里 **8/8 全中、零误报**。
**守卫**：无。这条是 B3「过量生成择优」的方法基础，写在主文档 §B3。

#### 1.4 同一条的第二个实例，比第一个更干净（2026-09-09）

**症状**：让 `deepseek-v4-flash` 通读一篇六级真题、「指出文章中所有的词组」，同一篇跑三次：
提出 37/28/34 个，**并集 50、交集 19，稳定率 38%**；只有 **21/50（42%）** 在 318,207 条词典里查得到；
**23 个在原文里根本找不到那个字符串**（`a you-and-me-against-the-world principle`、`strategic calculations`、`the cries of`）；
而且**连现有流水线已知的 13 个都漏了 3 个**。
**对照**：同一篇上，放宽后的结构筛提出 28 个、**全部在词典里、零编造**，其中 16 个是现有筛子漏的。
**正确做法**：**把候选锚在词典上**——词典里没有的组合它一个都提不出来，没有编造的余地。
**守卫**：无。数据在 `docs/phase-2.html` §18。

---

### 二、调模型 / 换模型前

#### 2.1 长文生成必须开流式

**症状**：写一篇 450 词的文章每次 524，而模型其实还在写。
**原因**：**网关掐的是沉默，不是长度**。gpt-5.5 要先思考约 109 秒才吐第一个可见 token，中转站在 120 秒掐断空闲连接。
**正确做法**：`llm_stream` 默认开（2026-09-09 加）。开了之后同一个请求 60 秒完成（453 词、超纲 0.85%、目标词 24/25）。
**关掉它长文生成直接不可用。**
**顺带**：中转站上非流式只有 `minimax-m2.5` 能写完整篇，但它目标词只命中 10/25、均句长 12.4；
其余模型把 2500 个输出 token 全烧在思维链上，正文 0 字。**能写完和守约束是两回事。**

#### 2.2 思考型模型会「成功地」返回空正文——流式也救不了（2026-09-10）

**症状**：`deepseek-v4.1-flash` 写文章四篇全空。流是通的、`finish_reason` 也有，就是没正文。
**原因**：`reasoning_tokens: 4000 / completion_tokens: 4000`，**delta 里只有 `reasoning_content`，`content` 一次都没出现**。
**正确做法**：三条解法——预算提到 16000（12.9s）、**`enable_thinking: false`（9.9s，最快）**、`thinking: {"type":"disabled"}`（13.1s）。
**排查手法记住**：模型「返回成功但没内容」时，**先把流里 delta 的字段名和 usage 打出来**——`reasoning_tokens` 会直接告诉你预算去哪了。

#### 2.3 别拿 star 数当权威依据

**症状**：`fsrs4anki` 有 4.1k star、`py-fsrs` 只有 481，差点据此选错。
**原因**：前者是**给 Anki 用的 JS 调度脚本 + notebook**（而且 FSRS 已内置进 Anki 23.10+，那个仓库对新版用户已无必要），
后者才是**能嵌进 Python 后端的库**。star 差的是用户群大小，不是权威性。
**正确做法**：**真正的权威证据是「FSRS 被 Anki 官方吸收成默认调度器」**，外加公开的 SRS benchmark 和十几种语言的独立实现。
**顺带**：许可要逐个核对。`py-fsrs` 是 MIT；**Anki 本身是 AGPL，绝对不能抄它的代码**；
同一个组织（open-spaced-repetition）下的仓库许可不一，有 AGPL 也有 CC0、BSD。

#### 2.4 别把「写文章」和「造例句」的横评结论搞混

**症状**：两次横评结论相反，容易记岔。
**原因**：任务大小不同——写 350 词整篇文章 vs 写一句话带一个指定的词。
**结论**：写文章 `gpt-5.5` 完胜（同一套提示词，超纲率 0.32% 对 `deepseek-v4-flash` 的 3.64%）；
**造例句不适用这条**（2026-09-09 横评 23 个模型）：合格率 92–100%，`deepseek-v4-flash-0731` 96% 反而高于 `gpt-5.5` 94%。
**顺带**：`deepseek-v4-flash` 那次横评 20 个模型的结论是**数据整理要的是不思考的模型**——
思考型模型把输出预算烧在思维链上，强模型反而在中转站的 180 秒网关超时里跑不完。

---

### 三、写管理页 JS 前

#### 3.1 `hidden` 在管理页里不一定隐藏得了

**症状**：设了 `el.hidden = true`，按钮**失效了但没消失**——「看得见却点不动」，代码全对而屏幕在骗人。
**原因**：`base.html` 给 `button` 设了 `display: inline-block`，优先级高过浏览器自带的 `[hidden] { display: none }`。
**正确做法**：已在 `base.html` 加 `[hidden] { display: none !important; }`。
**这类最难查，因为没有任何东西出错。**
**守卫**：`verify_phase0.py` 4.10。

#### 3.2 管理页继承 `base.html` 的全局作用域，别用它已有的名字

**症状**：**页面加载正常、之后每次点击都 404**，症状看着还是「按钮点不动」。
**原因**：复习页定义了同名的 `api()` 走 `/v1/client`，被 base 那个走 `/v1/admin` 的覆盖。
已被占用的名字：`api` / `toast` / `humanBytes`。P2 的阅读页把 helper 叫 `client()` 所以没事。
**正确做法**：**页面脚本一律包进 IIFE。**
**守卫**：`verify_phase0.py` 4.9 会扫描并报出撞车。

#### 3.3 Web 页的调试办法：抽出 `<script>`，在 node 里重放点击序列

**症状**：P3 人工验收时「按钮点不动」，猜了两轮都没找到。
**正确做法**：把 `<script>` 抽出来，配假 DOM 和假服务端，在 node 里重放点击序列。**重放一次就看清了**——
包括「答案面板没打开时点『对了』照样会提交」这种眼睛看不出来的。
**顺带**：**开发期的页面要 `Cache-Control: no-store`**——缓存的旧 JS 看起来和「没修好」一模一样。

---

### 四、加验收项或新指标前

#### 4.1 验收脚本不许依赖「今天碰巧有数据」

**症状**：`verify_phase3` 起初走当天真实的到期队列，于是复习做完之后再跑就一片红。
**原因**：**只在某些日子能过的验收脚本等于没有。**
**正确做法**：自己种探针、用完删掉，**前后各清一次**——中断的上一轮也不会污染下一轮。

#### 4.2 任何新指标上线前，先拿一个已知答案的样本验证它数得对

**症状**：难度画像里的「超纲率」量出来四级真题比考研还难。
**正确做法**：真题的三个等级就是现成的已知答案，任何难度类指标都该先在它上面跑一遍，看单调性对不对。
（这次是在写进代码之前跑数才发现的；§1.2 那次是改了三轮提示词之后。）

#### 4.3 阴性对照 + 阳性对照，缺一个都得不出结论

**症状**：搭配检查改好提示词之后真题和生成文都判成 ~0，看着像「文章没问题」。
**原因**：**分不清是「没问题」还是「裁判瞎了」。**
**正确做法**：补上**阳性对照**——拿真题句子把 `make a decision` 改成 `do a decision` 种个已知错误。
补上之后才知道模型是好的、问法是坏的。

#### 4.4 算法要用仿真验，单元检查查不出标定错

**症状**：FSRS 接线有两个错（把「一次过」当 Easy、没关掉自带 learning steps），在 1.1–1.6 全过的时候一直存在。
**原因**：**它们不让任何东西报错，只让间隔排得不对**——8→66→180 天，而正确的是 2→11→46→163→180。
**正确做法**：**让仿真里的学习者按 FSRS 自己的曲线遗忘**，消掉模型分歧，剩下的只有接线——
实测到期可提取性 0.901 对目标 0.900，偏离 0.0005。
**再换一个不同形状的遗忘模型，一次过率掉到 0.28**——那不是 bug，正是 optimizer 存在的理由。

---

### 五、查词典 / 判大纲前

**这个字段栽了四次。**四次的共同点是：**已知答案的样本一直在手边，却没去对。**

#### 5.1 第四次，也是最贵的一次（2026-09-09）

**症状**：阅读页把 `quickly` 标成超纲词，还说「不用学会」。
**原因**：「这个词在不在大纲里」有**三个调用方**——生成校验器算超纲率、入库打 `beyond` 标记、难度画像算超纲词比例。
**三方各写了一遍，只有生成校验器是对的**。它做了四件事，另外两方只做了第一件：

1. 词条自己的标签（并英美变体）
2. **表面形的标签**（`datum` 无标签，页面上是 `data`）
3. **词典自带的变形表**（`teeth` / `phenomena` / `curricula` 还原失败，但 ECDICT 的 `exchange` 认得）
4. **A 级派生的词根，且要顺链走到底**（`nationality → national → nation`，一步不够）

**实测**：453 篇里 8,401 处超纲标记，**3,005 处（35.8%）的词根就在纲内**——`quickly` 30 处、`fully` 28 处、`cultural` 27 处。修完降到 4,158 处。
**正确做法**：`vocabulary/syllabus.py` 一个函数，**三方都调它**，且**返回布尔不返回标签集合**——
返回集合就意味着每个调用方自己写比较，而它们正是各自停在了不同的地方。
B 级派生单独回答（`light_root`），它不算超纲但算三分之一个目标词，两个答案要能分开数。
**重算**走 `reading/recompute.py`（`POST /v1/admin/reading/recompute`），**只重写词典派生出来的值，
绝不碰 token 的身份、位置、义项标注**——那些是遇见记录，标记指着它们；前后各数一次行数，对不上就回滚。
**这次比前三次多学到一条：坏规则不只是让数字难看，它会让你选错。**
P3 的模型横评先用坏规则算，排出 gpt-5.5 92% > deepseek-v4-flash 88%；换成对的规则重算变成
**96% 对 94%，名次反了**——差一点就把活派给了贵四倍、慢四倍的模型。
**守卫**：`verify_phase2.py` 3.6（`quickly` / `teeth` / `nationality` 不许判超纲）与 **3.7（读源码断言三方调的是同一个函数）**。

#### 5.2 第三次：`tags` 在英式／美式拼写之间随机分配（2026-09-08）

**症状**：`labor` 被判成超纲——实测 21 个词条、125 处，`labor` 一个词 41 处。
**原因**：ECDICT 的 `tags` **不是各存一份**：`labour` 带 `zk gk cet4 cet6`，而 `labor` 只带 `ky`；
`judgement` 是四级词，`judgment` 一个标签都没有。
**正确做法**：`vocabulary/spelling.py` 和 `tags_of()` **就是为这件事写的**，模块开头第一段写的正是这个问题——调它。
（当时判超纲的两处 `ingest._is_beyond`、`difficulty.syllabus_tags` 没有调，安静地过了整整一个阶段。）
**守卫**：`verify_phase2.py` 3.5。

#### 5.3 第二次：`tags` 是分级清单，不是累积的（2026-09-07）

**症状**：超纲率 58%，四级真题量出来比考研还难。
**原因**：`the` 只带 `zk gk`（中考、高考），**并不带 `cet4`**；`make`、`people` 同理。
**正确做法**：「四级水平之内」= 带 `zk` / `gk` / `cet4` **任意一个**。改对之后超纲率 58% → 10%，三个等级也变成正确的单调上升。

#### 5.4 词形还原式和原样式各查一次

**症状**：漏义项报告里排第一的词是 `accord`，追下去是 **84 处 `according to` 一处都没识别出来**。
**原因**：spaCy 把 `according` 还原成 `accord`，而 `accord to` 在词典里没有条目。
**正确做法**：查词典时**两种形式各查一次**。
**顺带**：**报告本身就是诊断工具**——它排第一的那个词指的不是缺一条义项，而是识别少了一整类。

---

### 六、写数据库代码 / 解析别处写的数据前

#### 6.1 凡是解析别处写的数据，先看一眼它长什么样

**症状**：文章照样能读，**读完什么都不记**。不报错。
**原因**：`generation_drafts.target_words` 存的是 JSON 数组，按逗号空格切会切出 `["assignment"` 这种永远匹配不上的东西。
**正确做法**：**别照着字段名想当然**，先打印一条真实数据出来看。

#### 6.2 `sqlite3.IntegrityError` 被捕获后必须回滚

**症状**：几步之后一个**毫不相干**的请求报「database is locked」。
**原因**：失败的 INSERT 会把隐式事务挂着不放写锁。
**正确做法**：**吞掉数据库异常的地方都要问一句事务归谁收。**

---

### 七、读代码时、等后台任务时

#### 7.1 一段解释性的注释会让人以为对应的代码在那儿

**症状**：404 带着 trace_id 发给用户、什么也不写；用户把 trace 贴过来，**查出来是空的**。
而「贴 trace 就能定位」正是把 trace_id 放进响应的全部理由。
**原因**：`core/errors.py` 的 404 处理器上写着「保持 INFO 级：拼错地址不算事故」，**下面没有任何一行 log 调用**。
**正确做法**：读代码时**不要把注释当成实现的描述**——它描述的是意图，实现可能从来没跟上。
**守卫**：`verify_phase0.py` 9.4。

#### 7.2 等待条件不要依赖一个后来又被自己改变的东西

**症状**：循环永远不退出，shell 一直空转，控制台一直显示「还有任务在跑」，让人以为有事没做完。
**原因**：犯过两次同一类错误——第一次是等一个永远凑不满的输出行数（自己用 `tail` 截掉了前几行），
第二次是等一个日志文件里的完成标记（自己在收尾时 `rm` 掉了那个文件）。
**正确做法**：见上面「后台任务与等待」那四条规则（查数据库不轮询日志、循环必须有超时、清理放最后、收尾查残留进程）。

---

### 八、跨 Phase 的静默错误清单（只有一行，但都真的发生过）

- **入库绝不写 `study_states`**：破了它，没读过的文章里的词会被静默记为已学、从此不再出现——没有报错、没有日志。守卫 `verify_phase2.py` 7.1。
- **撤销标记后要退回 `new`**，但**遇见记录保留**（它确实被遇见过）。守卫 7.13。
- **换义项集时不要重新标注**：`store_senses` 是删了重插，义项 id 全变，一次全量替换会静默作废 95,058 条标注（460 万输入 token）和全部考频。做义项级映射，约 7,200 次调用。
- **词组只高亮半个词**：第一版点任一半都开面板，但高亮只亮半个词、标记后正文毫无反应。**高亮半个词，等于用更安静的方式说了同一句错话。**
- **方案写了但代码没写**：「读完文章自动补句子池」先前写进了方案没写进代码，拨到第二天才发现「抽到的题没有句子」。守卫 `verify_phase3.py` 5.5。
- **验收脚本会到期**：`verify_phase2.py` 7.5 原本断言「调度字段不存在」，P3 做了之后这条到期。改成守始终成立的那一半（入库与阅读都不许碰调度字段）。**删掉会丢守卫，留着会天天报假警。**
