# English Reader

面向 CET-4 / CET-6 的英语阅读学习 App，单人自用。核心机制是从阅读中习得词汇，并用真实语境安排复习。

## 文档

- `docs/english-reader.html` — 功能设计（做什么、为什么这么做、开发路线、架构前提）
- `docs/phase-0.html` — P0 实施方案（八项技术定案、要建的东西、明确不做的边界、验收清单）
- `docs/phase-1.html` — P1 方案（三段推进、已定决策、P1a 工作流、验收标准）
- `docs/phase-1b.html` — P1b 方案（API 集成、词典修复、义项集、词族、生成改进）
- `docs/phase-1c.html` — P1c 方案（用 Wiktionary 清单 + 真题考频重建义项集）
- `docs/phase-2.html` — P2 方案（文章入库、客户端契约、点词查词、三档词状态；**讨论中**）

**文档只存在本地，不要发布为 Artifact。** 用户直接用浏览器打开这些 HTML 文件查看——所有样式和交互都在文件内，不依赖网络。

**文档组织规范**：功能设计集中在主文档，每个 Phase 的实施方案各自一份 `docs/phase-N.html`。讨论产出的结论更新进对应文档，不要随意另开新文档。

## 仓库

https://github.com/SCmenghua/english-reader （私有，默认分支 `main`）

代码进版本控制，数据不进：`data/` 下的数据库、词典源文件、`.env`（含管理密码）、`data/secrets.json`（API 密钥）全部在 `.gitignore` 里。备份走管理控制台的下载按钮，不走 git。

数据库按「丢了会怎样」分两类：**必须备份**——`learning.db`（学习记录）与 `content.db`（花钱生成的义项集、例句、词族，P1b 建立）；**不用备份**——`dictionary.db`（可从 ECDICT 重建）与 `logs.db`（可丢弃）。

## 开发流程

按 Phase 推进，路线见主文档 §K。

**P2 阅读闭环 —— 已于 2026-09-08 验收通过并提交。含 P2y 词组。**

**原 P3–P8 已于 2026-09-08 整体归档**（主文档 §K 表内原样留着，表后写了归档的两个理由）。
归档的起因：P2 吃掉了原本排在后面的一大半（语境标注、难度画像、第三层释义、词组、三档标记、客户端契约），
而且原路线按「能力」切，缺的东西要按「实际在跑的那个循环」切才看得清。
**代码与 P2 契约里那些写着 P3／P5 的注释和占位字段，留的位置不变，只是「谁来填」重新排过了。**

**2026-09-09 起只规划当前 Phase，不提前规划未来。** §K 表里只有「已完成的」和「当前这个」。
发现的未来需求记进主文档 §M 待定，不动手。

**当前是 Phase 3 = 复习**（方案 `docs/phase-3.html`，**已定稿，尚未动手**）。
一句话：标记的词按记忆状态自己回来。**二十四条决定在该文档 §2，§9 已经空了。**
挑几条最容易被忘掉的记在这里：

- **双向必做**，两个方向的名字是**看词想义**（句子里的词高亮 → 想它在这里什么意思）和
  **看义想词**（句子挖空 + 给中文义项 → 想填哪个词）。
  **原来叫「方向一／方向二」，2026-09-09 改掉了**——位置不是内容，隔天就想不起来指什么，
  跟 P2y 那个名字栽的是同一个坑。
  **语境是题目的一部分不是提示**——按义项分之后，光给一个单词指不出问的是哪个意思
- **看义想词给中文，挖空填句中的实际形式**（`addressed` 不是 `address`）。
  它也有四级提示，**第 1 级是首字母**——专治「着手解决也对得上 deal with」的歧义。
  歧义之所以不致命，是因为判定本来就是自评：这一步是**练习不是检验**，换成四选一才会变成硬伤
- **「不认识」当天就复习一次，「模糊」当天不来、第二天开始。**
  P2 定的「模糊只记录不调度」到此为止——那句话的前提是 P2 根本没有调度
- **两个池子分开记**（当天标记的 / 算法到期的），**怎么呈现留着以后选**（混着抽／先后／双入口）
- **句子读完文章那一刻就生成**，夜间批次补足并往前建。**句库是长期资产**：
  全量 13,741 义项 × 8 句 ≈ 11 万句、13,741 次调用、20 rpm 下约 11.5 小时、9 MB——算得起
- **题面用你没见过的句子，原句退居提示位**。每义项备 5–10 条随机抽，
  因为只见过一句的话记住的是那句话、换一句又不认识。**实测语料库中位数只有 3 条，所以 AI 生成是主力不是补充**
- **当天不会就重复到会**，但**不定次数上限**——改用伪随机降权，错得越多越抽不到，卡住的词自己让路。
  看词想义过了**放回池子**而不是接着问看义想词：刚看完答案就问考的是几秒钟的短期记忆
- **评分双维度加权**：当天问了几次才过 + 累计复习过几轮
- **绝对不要四选一**（给了选项就能排除法蒙），判定靠自评
- **拼写只作强化选项**：当天复习完之后可选，针对当天所有词、按词去重；**拼错不影响调度**，
  只落一条「哪个词、对没对、拼错成了什么」的记录，**只写不读**，留给将来主攻拼写的功能
- **P3 的复习只做单词，词组不做**（2026-09-09 定，理由在 phase-3.html §7）。
  「不给错释义」和「把词组当学习对象」是两个目的：前者 P2y 已完成、价值确凿（每篇约 5 处）；
  后者从未被单独论证过，现有证据只有**12 条标记里 1 条是词组**，而它是 P3 里最贵的一块。
  **已知缺口**：`stick to` 现在就在 `reviewing` 池里且不会被复习——验收有一条专门查它
  **是被有意跳过的，不是被静默漏掉的**
- **调度用 FSRS，依赖 `py-fsrs`（PyPI 名 `fsrs`，MIT，v6.3.2，调度器无第三方依赖），不自己抄公式**——
  v6 有 21 个默认参数，**记错一个就是一个安静的错误**，这个项目已经三次栽在这上面。
  **Anki 本身是 AGPL，绝对不能抄它的代码**（会传染整个项目）；同组织下的仓库许可不一，逐个核对
- **别拿 star 数当权威依据。** `fsrs4anki` 有 4.1k star、`py-fsrs` 只有 481，但前者是**给 Anki 用的
  JS 调度脚本 + notebook**（而且 FSRS 已内置进 Anki 23.10+，那个仓库对新版用户已无必要），
  后者才是**能嵌进 Python 后端的库**。star 差的是用户群大小，不是权威性。
  **真正的权威证据是「FSRS 被 Anki 官方吸收成默认调度器」**，外加公开的 SRS benchmark 和十几种语言的独立实现
- **FSRS 的 D／S／due 正好对上我们留的「难度／记忆强度／下次到期」，一个字段都不用改。**
  「累计轮数」**不要再写加权公式**——它是 D 的结果不是输入，写了就是重复计数（决定 11 的措辞已按这条修正）
- **关掉 FSRS 自带的 learning steps。** 它有自己的「当天多次重复」机制，而我们已经用伪随机降权做了同一件事，
  两个都开会重复计数。**口径：当天整体算一次复习，评分反映当天重复了几次**
  （1 次过 Easy／2 次 Good／3 次 Hard／4 次及以上 Again）
- **optimizer 先不装**（是可选依赖 `fsrs[optimizer]`），没数据时做了没用；
  先用默认参数，攒几个月历史再拟合成你自己的曲线。**换库、换算法的风险已被架构挡掉**：
  状态存自己表里、调度器是纯函数、历史完整落表，随时能重放
- **不做**：水平估计、选文、今日包、读后抽查、把到期词塞进文章、毕业判定、手机端、进度统计

主文档同日改了四处：删掉「每日投入 30 分钟」（§I 加了「每日总时长 不限」并记了为什么——
是时间预算在削学习量）、§H 流程里的分钟数、§J 把拼写从「产出型不做」里拆出来单列「改了」、
§M 加了「每日复习量负载均衡算法待优化」。
**P2 验收之后又修了一个缺陷（2026-09-09，记在 `docs/phase-2.html` §17）**：超纲判定漏了三步，
详见下面「踩过坑的经验」的第四条。验收脚本从 45 项变成 **47 项**。

**P2 的**方案与实施结果见 `docs/phase-2.html`，它分两部分：**§1–§12 是 P2 阅读闭环**
（§2 十四条决定，§10 七项待定的结论与配置项，§11 验收标准，§12 实施结果——含写完才发现的缺陷与标定数据），
**§13 是 P2y 词组**（自带方案、六步实施顺序、七条验收与实施结果），§14 是待你定夺的事。

**「P2y」= P2 之内的词组工作**（2026-09-07 定案，09-08 实施），不是路线图上的一个 Phase。
这个名字先前只活在对话里，没写进文档，结果隔天连它指什么都要重新对一遍——所以记在这里。
真正的 **Phase 2x 是另一回事，已于 2026-09-07 取消**，两者别搞混。

自动验收 `uv run python scripts/verify_phase2.py`：**45 项全过**；
人工验收 6 项（M1 读完一篇 · M2 四个分支 · M2b 词组 · M3 同词不同义项 · M4 离线 · M5 第三层释义）
**2026-09-08 全部通过**。操作手册见 `docs/phase-2.html` §15，验收当天改出来的两件事见 §16。
回归：P0 29 项、P1b 28 项、P1c 16 项，全过。

新增模块 `reading`：八张表、第一套 `/v1/client` 接口、两个管理页（`/admin/reading` 文章库、
`/admin/reader` 开发期阅读页）、两个批次任务（语境标注、词组判断）、八个配置项。
**模块外只动了两处**：`devices` 表加 `learner_id` 列（身份口子，由 core auth 添加，因为表是它的），
以及 `analyzer.py` 修断句缺陷 + 生成工作台加「入库」按钮。

**两条关键不变量，改动时别破：**

- **记账只在读完那一刻发生，入库绝不写 `study_states`（原 `sense_states`）。**
  破了它，没读过的文章里的词会被
  静默记为已学、从此不再出现——没有报错、没有日志。`verify_phase2.py` 的 7.1 专门查这条。
- **进复习队列只有两条路，都是你自己的信号：你标记了，或读后抽查答错了（D3）。**
  推论（2026-09-08 M2 验收补上）：**超纲词也能标、也能进队列**。原先那个分支不给标记按钮，
  等于系统替你决定了你不许想学它。「不在大纲里」是关于大纲的事实，「不许学」是替你做主。
  不计生词率照旧——那个数描述的是文章对大纲，不是文章对你。
  两者都没有就不进。**读完一篇文章只增加遇见次数，不改变词池位置**——
  不管是真题还是生成文章，也不管这篇是不是为教那些词而写的。
  系统不替你判断你会不会：那是水平估计，属于 A2／A4，不该由「读完」这个动作代劳。
  试过两版自动记账（真题按词频挑 25 个、生成文按声明的目标词入账），都推翻了。
  **代价要认**：主文档「25 词 × 289 篇 ≈ 3.2 个月覆盖」的账原本假定读完即学会，
  现在覆盖速度取决于标记率，不再可预测。`verify_phase2.py` 的 7.4 查这条不变量。

**P2x 已取消**（2026-09-07）：定点修补砍掉（合格率过半，丢弃比修便宜），
剩下的「过量生成择优」降格并入 **P6**——方法已验证、无未知数，
而真正需要每天可靠产出文章的正是 P6。做法写在主文档 §B3，不另开实施方案文档。
**关键做法：绝不让模型「找错」，只让它逐句做封闭判断**（见下面「踩过坑的经验」）。
实测基线：真题 1.0% 的句子被判不自然，生成文 2.9%，10 篇里 6 篇零问题——
这个数字同时回答了 P1 那条一直悬着的完成定义「搭配无误」。

**真题考频已算出**（2026-09-07，2026-09-08 按词组清账后重算为 7107 个义项）：
452 篇全量标注跑完后，义项有了真题出现次数，
第三层释义转为可用。验证性的例子——`address`「地址」出现 2 次、「着手解决」38 次，
正是 p1c §3 当初拿来举例预言的那一个。重算的入口：`POST /v1/admin/reading/exam-frequency`，可重复跑。

**但 `is_exam_key` 熟词僻义标记暂不启用，保持为 0。** 规则（序号靠后却被考到）本身成立，
但它依赖可信的义项排序，而现在的序号是模型猜的——按这条规则会把 `well`「好」、`even`「甚至」
标成熟词僻义，占全部义项的 19%。等义项集换成真正的词典、排序有词频分级依据了再启用。
在那之前只用考频本身，界面上并列显示同一个词各义项的次数，让读者自己看出对比。

**换义项集时不要重新标注**（主文档 §F4 已记）：现在的义项是模型写的，将来会换。
但 `store_senses` 是删了重插，义项 id 全变，而每篇文章的每个实词都指着它们——
一次全量替换会静默作废 95058 条标注（460 万输入 token 的代价）和全部考频。
正确做法是**保留旧表、做义项级映射**（约 7200 次调用，对比逐 token 重标的 95058 个 token）。
今天做的 `senses.replaced` 事件是给**单词补全**用的，不是给换词典用的，这个区别别搞混。

**词组（P2y）：已实施（2026-09-08），七条验收全过。** 方案与实施结果见 `docs/phase-2.html` §13。
先前曾未经充分讨论就实现并提交，随后整体撤回；这一版是逐条谈完之后才动手的。

实测：词典 318,207 条词组；453 篇扫出 3,191 处候选（每篇 7.0，噪音 0 处）；
161 次调用判完，2,291 处判为词组（777 个不同）、900 处判为字面用法；
**198 个序列在不同句子里得到不同判定**（`look at` 21/20、`come from` 15/15、`live in` 3/35）——
这就是「句子在起作用、不是在匹配字符串」的证据。清账给 4,773 个 token 打了标记，
真题考频从 7,203 个义项降到 **7,107**，漏义项报告降到 124 个词、310 处。

**踩到的一类漏识别，值得记住：**漏义项报告里排第一的词是 `accord`，追下去是
**84 处 `according to` 一处都没识别出来**——spaCy 把 `according` 还原成 `accord`，
而 `accord to` 在词典里没有条目。修法是查词典时**词形还原式和原样式各查一次**。
**报告本身就是诊断工具**：它排第一的那个词指的不是缺一条义项，而是识别少了一整类。

**顺带修的一条不变量缺口**：撤销标记后 `study_states` 仍留在 `reviewing`，
违反「进队列只由你的信号决定」——信号收回，条目就该退回 `new`，**但遇见记录保留**
（它确实被遇见过）。`verify_phase2.py` 的 7.13 查这条。

定下来的六条（都已实现）：
- **不建「哪些词组值得学」的清单**——不认识就标，跟单词一样。与「进复习队列只由你的标记决定」同源。
  「31.8 万条词组只有 6 条带考纲标签」是策展的难题，而我们不策展
- **识别分两步**：结构性预筛（动词+小品词，词典里有这个组合，不调模型，挡掉 `to be` 这类噪音，
  每篇从 43 处降到 6.8 处）+ 逐处封闭判断（「在这句里是不是一个整体」，判据是**透明度不是价值**）
- **学习记录合表**：`word_marks`／`sense_states` 已改名 `study_marks`／`study_states`，
  加 `item_type`（`CHECK` 只允许 word／phrase）+ `item_key`。合并的理由是**复习调度要混着排序**，
  分表就要把核心算法写两遍。迁移写成函数而不是一段 SQL，因为它**前后各数一次行数、对不上就回滚**
- **词典分表**：词组在单独的 `phrases` 表（`dictionary.db`，318,207 条）。判据同上但答案相反——
  **没有查询需要不分类型取全部条目**，而且主表 `words` 不该从 40 万涨到 72 万。
  导入合进 `import_dictionary.py`，`--phrases-only` 只重建词组（4 秒）
- **顺带清账**：落在词组里的 token 打 `in_phrase` 标记 + 考频重算。实测 4,773 个 token 受影响，
  而**换词典不会洗掉它**——§F4 定的映射法保留旧标注，也就保留错误。
  漏义项报告同样按这个标记过滤，`get up`／`at least` 自己消失了
- **标记词组后追问组成词**：「那 account 本身呢？」逐义项可单独标。
  不认识词组不代表不认识组成词，让词组的标记替它背锅会把一个掌握的词塞进复习队列

**词组是一个整体，就按一个整体渲染——这条对所有客户端都成立。**
静止状态正文里不加任何记号（下划线三种、底色两种已占满，先前那版给词组加底色
实测让 28 个超纲词、34 个派生词丢了标记）；但**悬停、点选、标记都整体发生**，
一处词组渲染成一个元素，两个词连同中间的空格都在里面。
第一版只做对了一半——点任一半都开面板，但高亮只亮半个词、标记后正文毫无反应，
M2b 验收当场被指出来。**高亮半个词，等于用更安静的方式说了同一句错话。**
静止要不要标出来仍是各客户端自己的呈现决定，「选中与标记按整体」不是。

**词组不卡水平估计**（它按单词算）。先做词组的理由是**欠账每读一篇涨一次**，不是有技术依赖——
这条理由现在同样指向复习：标记的词沉进池底再无下文，也是每读一篇涨一次。

P2 讨论中改动了主文档的三处，已同步：§K 的 P2 那一行（不做今日包、完成定义从「手机上」改成「电脑上」）、
§C4（标记生词扩成三档）、§F3（掌握状态带上三档标记）。

**设计 P2 的客户端接口时，留下「这是谁的数据」这个口子。** 铁律 5 是只增不减，
而 P2 正是定义客户端契约的那一刻——若把「只有一个人」焊死进契约，
将来要给别人用就只剩开 `/v2/` 或打补丁两条路。不实现注册登录，只要协议层面说得通。
定案：身份由设备令牌推导，响应顶层回显 `learner`，路径里不出现人的编号。
理由与其余远期事项（多用户、合规、生成成本）记在主文档 §M 末尾。

**这个口子不止于身份。** 凡是将来某个 Phase 才会填上的东西——水平估计、义项的记忆状态、
专有名词的说明——**字段在 P2 的契约里就要有位置，值先为空**，并由响应顶层的 `capabilities`
声明区分「没数据」和「没实现」，客户端据此渲染「待开发」而不是空白。
但**只留形状显而易见的位置**（布尔、一句话、一个数、可整体为 null 的对象）；
复习卡片、抽查题、今日包这类结构复杂的不猜，将来以**新增端点**接入。详见 `docs/phase-2.html` §6。

**Web 阅读页与手机无关。** 它只在电脑上跑，是开发期测试工具（铁律 8）——不做响应式、
不做触摸手势、不做排版调节、不测手机浏览器。手机上要用的是 P7 的 iOS 原生端。
P2 的验收是「电脑上真读完一篇」，主文档 §K 里原来那句「手机上」已经改掉。

P1b 与 P1c 已于 2026-09-06 一并验收通过并提交。验收脚本：
`verify_phase0.py` 29 项、`verify_phase1b.py` 28 项、`verify_phase1c.py` 16 项，全过。

顺延的四项（记在 `docs/phase-1c.html` §13，主文档 §K 路线表已更新）：

- **语境释义标注**、**真题难度画像** → **P2**。消费方都在 P2（点词查词第一层、真题按水平筛选）。
  在使用它们的功能出现之前先做，就是在没有反馈的情况下猜格式。
- **过量生成择优**、**定点修补** → **P2x**（新增阶段，排在 P2 之后）。
  不阻塞 P2：P2 读的是已经生成好的文章，而超纲率已经 0.09% 没有要修的；
  反过来 AI 搭配检查需要 P2 的真实阅读来提供金标准。

**P1 完成定义有一条没验证**：「搭配无误」——盲测按决定取消了，目前只有本人试读的主观判断。
不要把它当成验证过的事。

**模型分工（实测结论，别搞反）**：数据整理用 `deepseek-v4-flash`（快、便宜、准），
**写文章用 `gpt-5.5`**——同一套提示词，超纲率 3.64% 对 0.32%。写作和数据整理要的能力不一样。
**但「给一个义项造例句」不适用这条**（2026-09-09 横评 23 个模型，见 `docs/phase-3.html` §11）：
合格率 92–100%，`deepseek-v4-flash-0731` 96% 反而高于 `gpt-5.5` 94%。
区别在任务大小——写 350 词整篇文章 vs 写一句话带一个指定的词。**别把两个结论搞混。**
中转站限速 **20 rpm**，这条比模型延迟更决定批次总耗时。
`seed-2.1-*` 与 `qwen3.8-max` 在中转站全部 504（60 秒网关就断），`minimax-m2.7` 全部 429。
生成用哪个提供商由配置项 `gen_provider` 指定，不走「默认提供商」。
`shuai` 提供商额度有限，**当前已停用**，需要时在控制台启用。

P1 完成定义「生成的文章用词在范围内、句法是正常书面语」已验证达标（详见 `docs/phase-1c.html` §12）。
gpt-5.5 实测：超纲率 0.14%、被动 15.5（基线 17.0）、名词化 16.3、句长 18.8（基线 18.9）。

**生词密度 2026-09 上调三倍**：每篇目标词 8 → **25 个**，要求**每段 5–6 个、分布均匀**。
按 8 个算，覆盖 7226 个目标词要读 903 篇、每天三篇十个月；25 个是 289 篇、3.2 个月。
实测密度提高三倍后超纲率仍只有 0.29%，每段分布 4–9 个无空段。
**硬凑的用法（为了用上某个词临时加一句）已明确接受**，不要再花力气修——
用户的原话是「硬凑就硬凑吧，无所谓」，功利地看把词搞懂比句句自然重要。

**两条踩过坑的经验，改提示词前先读：**

- **模型对提示词里的数量约束基本无感。**「不要超过五六处」无效（反而更高）、
  「至少用一两处」无效、义项集那次「合并成 3-5 项」也无效（给了 7 项）。
  **说清楚「什么时候该用」才有效**——被动语态就是靠给条件 + 写完自查，从 30.8 降到 15.5 的。
- **一个错误的度量比没有度量更危险。**「形式主语几乎为零」让我改了三轮提示词，
  实际是校验器在数 `there be`：spaCy 只把 `there is/are` 标成 `expl`，
  `It is clear that…` 里的 `It` 是普通 `nsubj`。改对之后发现真实值是 8.91，基线 5.6，
  一直是偏多而不是为零。**改代码前先验证指标本身在数什么。**

- **让模型「找错」是生成任务，它会编造；让它判断具体对象是封闭任务，它很准。**
  同一个 `deepseek-v4-flash`，同一件事，问法不同差到离谱：
  问「找出这篇文章里不地道的搭配」，它给**考试委员会审定的真题**判出均值 5.1 个「问题」
  （`exerting influence on` 要改成 `having influence on`，`gap remains yawning` 要改成 `wide`）；
  换成逐句问「这句话母语者会这么说吗」，在种下已知错误的对照测试里 **8/8 全中、零误报**。
  开放式「找错」有产出压力，模型总能挤出点什么。**要判断就给它具体对象，别让它自己去找。**

- **「开放式找错」那条的第二个实例，比第一个更干净（2026-09-09）。** 让 `deepseek-v4-flash`
  通读一篇六级真题、「指出文章中所有的词组」，同一篇跑三次：提出 37/28/34 个，
  **并集 50、交集 19，稳定率 38%**；只有 **21/50（42%）**在 318,207 条词典里查得到；
  **23 个在原文里根本找不到那个字符串**（`a you-and-me-against-the-world principle`、
  `strategic calculations`、`the cries of`）；而且**连现有流水线已知的 13 个都漏了 3 个**。
  同一篇上，放宽后的结构筛提出 28 个、**全部在词典里、零编造**，其中 16 个是现有筛子漏的。
  **锚在词典上的做法没有编造的余地**——词典里没有的组合它一个都提不出来。
  数据在 `docs/phase-2.html` §18。

- **一个测试在已知好的样本上报零，不等于它有效——还得看它在已知坏的样本上报不报。**
  搭配检查改好提示词之后真题和生成文都判成 ~0，看着像"文章没问题"，其实分不清是
  "没问题"还是"裁判瞎了"。补上**阳性对照**（拿真题句子把 `make a decision` 改成 `do a decision`）
  才知道模型是好的、问法是坏的。**阴性对照 + 阳性对照，缺一个都得不出结论。**

- **第四次，也是最贵的一次（2026-09-09，P3 讨论中量句子池时顺手发现）。**
  「这个词在不在大纲里」有**三个调用方**：生成校验器算超纲率、入库打 `beyond` 标记、难度画像算超纲词比例。
  **三方各写了一遍，只有生成校验器是对的**——它做了四件事，另外两方只做了第一件：
  ①词条自己的标签（并英美变体）②**表面形的标签**（`datum` 无标签，页面上是 `data`）
  ③**词典自带的变形表**（`teeth`／`phenomena`／`curricula` 还原失败，但 ECDICT 的 `exchange` 认得）
  ④**A 级派生的词根，且要顺链走到底**（`nationality → national → nation`，一步不够）。
  **实测：453 篇里 8,401 处超纲标记，3,005 处（35.8%）的词根就在纲内**——
  `quickly` 30 处、`fully` 28 处、`cultural` 27 处。阅读页把 `quickly` 标成超纲词还说「不用学会」。
  修法：新建 `vocabulary/syllabus.py`，**三方都调它**，且**返回布尔不返回标签集合**——
  返回集合就意味着每个调用方自己写比较，而它们正是各自停在了不同的地方。
  B 级派生单独回答（`light_root`），因为它不算超纲但算三分之一个目标词，两个答案要能分开数。
  重算走 `reading/recompute.py`（`POST /v1/admin/reading/recompute`），**只重写词典派生出来的值，
  绝不碰 token 的身份、位置、义项标注**——那些是遇见记录，标记指着它们；前后各数一次行数，对不上就回滚。
  结果 8,401 → **4,158**，三个考试等级仍然单调（超四级率 6.18 / 8.97 / 8.96）。
  验收脚本新增 3.6（`quickly`／`teeth`／`nationality` 不许判超纲）与 **3.7（读源码断言三方调的是同一个函数）**。
  **这次比前三次多学到一条：坏规则不只是让数字难看，它会让你选错。**
  P3 的模型横评先用坏规则算，排出 gpt-5.5 92% > deepseek-v4-flash 88%；换成对的规则重算变成
  **96% 对 94%，名次反了**——差一点就把活派给了贵四倍、慢四倍的模型。

- **第三次栽在同一个字段上（2026-09-08，P2 人工验收前）。** ECDICT 的 `tags`
  **在英式／美式拼写之间随机分配**，不是各存一份：`labour` 带 `zk gk cet4 cet6`，
  而 `labor` 只带 `ky`；`judgement` 是四级词，`judgment` 一个标签都没有。
  只读一边就会把 `labor` 判成超纲——**实测 21 个词条、125 处，`labor` 一个词 41 处**。
  项目里 `vocabulary/spelling.py` 和 `tags_of()` **就是为这件事写的**，模块开头第一段写的正是这个问题，
  但判超纲的两处（`ingest._is_beyond`、`difficulty.syllabus_tags`）没有调它。
  **已知答案的样本一直在手边**：拿 `labor` 和 `labour` 对一下就当场露馅，
  而它安静地过了整整一个阶段。现在写进验收脚本 3.5，不再靠记性。

- **这条已经栽过第二次了（2026-09-07，P2 讨论中）。** 难度画像里的「超纲率」原本写成
  「`tags` 里不含 `cet4`」，量出来四级真题比考研还难。原因是 **ECDICT 的 `tags` 是分级清单，不是累积的**——
  `the` 只带 `zk gk`，不带 `cet4`；`make`、`people` 同理。所以「四级水平之内」= 带 `zk`／`gk`／`cet4`
  **任意一个**。改对之后超纲率从 58% 变成 10%，三个等级也变成正确的单调上升。
  **任何新指标上线前，先拿一个已知答案的样本验证它数得对**——真题的三个等级就是现成的已知答案。
  这次是在写进代码之前跑数才发现的，上次是改了三轮提示词之后。

原 P1c 的「过量生成择优」「定点修补」「语境释义标注」顺延到 **P2x**；
其中语境释义标注是 P2 点词查词的直接依赖，可以跟 P2 一起做。

P1b 与 P1c 都未提交（按用户要求，两个 Phase 一起提交）。

**P2 踩到的第三类静默错误（前两次记在下面的「踩过坑的经验」里）：**
`generation_drafts.target_words` 存的是 JSON 数组，按逗号空格切会切出 `["assignment"` 这种
永远匹配不上的东西。后果不是报错，是文章照样能读、读完什么都不记。
**凡是解析别处写的数据，先看一眼它长什么样，别照着字段名想当然。**

另一条：`sqlite3.IntegrityError` 被捕获后**必须回滚**。失败的 INSERT 会把隐式事务挂着不放写锁，
症状是几步之后一个毫不相干的请求报「database is locked」。吞掉数据库异常的地方都要问一句事务归谁收。

P1c 的实际走向见 `docs/phase-1c.html` §11：原计划用 Wiktionary 清单重建义项集，
前三步跑通（覆盖率 99%；证实原粗筛跳过的词里 65% 其实是多义词），
但**第四步的粒度控制没做通**——给模型清单后平均义项数从 2.46 涨到 5–7，还出现重复义项。
诊断是「读 25 条清单做语义聚类」超出了快模型的能力，需要改成两步走（先聚类、再写定义）。

**决定：重建搁置，先用老办法补全，把闭环推下去。** 现在 7227 个目标词全部有义项
（13739 条，平均 1.90，1–3 个义项的占 92%）。粗筛那道过滤已删除——它判英文多义靠数中文逗号。

留在代码里随时能接的：`wiktionary_senses` 表、`sense_rebuild` 任务、`inventory.py` 的分词性截断。
另外**真题义项标注**（考频、熟词僻义标记、漏义项报告）设计在 §3，不依赖重建，可独立做。

关键约束（重建时别忘）：**不要问模型「哪些义项值得学」**——它会答最常见的，
而四六级考的是熟词僻义。只能问「这个义项属不属于通用书面英语」。

自动验收 `uv run python scripts/verify_phase1b.py`（28 项全过，3 项待人工）。
实施结果记在 `docs/phase-1b.html` §9，接真模型才暴露的问题在 §10，选模型与全量跑完的结果在 §11。

数据已生成：义项集 3581 词 / 8819 义项，词族 10471 条（近半经模型复核），词缀说明 216 条。
用的是 `deepseek-v4-flash-0731`——横评 20 个模型的结论是**这些任务要的是不思考的模型**，
思考型模型把输出预算烧在思维链上，强模型反而在中转站的 180 秒网关超时里跑不完。

新增四个模块：`llm`（提供商、密钥、批量任务、成本上限）、`wordfamily`（词根词缀与 A/B/C 分级）、
`senses`（义项集与三层验证），以及第四个数据库 `content.db`。控制台新增三页：模型 / 词族 / 义项。

P1a 已于 2026-09-05 验收通过：8 个模型 × 2 组目标词共 16 篇，核心假设成立。
结论与实验数据见 `docs/phase-1.html` §6。真题语料 452 篇在 `data/exam_papers/`，按考试分开。
生成工作台在 `/admin/generation`（手动模式：出提示词 → 贴到网页版 AI → 贴回结果 → 看体检报告）。

P0 已于 2026-09-05 完成并验收（自动检查 29 项全过，见 `docs/phase-0.html`）。
服务用 `start.bat` 启动，开发用 `dev.bat`；`uv run python scripts/verify_phase0.py` 可复跑 P0 验收。
Docker 部署按决定推迟到整个项目开发完毕，开发全程在 Windows 本机进行。

铁律：

- **绝不跨 Phase 开发。** 不实现任何属于未来 Phase 的功能，哪怕只是"顺手做了"。P0 的禁做清单见 `docs/phase-0.html` §4。
- 当前 Phase 中发现的未来需求，记进待办，不动手。
- 数据结构**可以**为未来预留字段，但不实现任何未来 Phase 的逻辑。这是"留位置"，不是"提前开发"。
- **每个 Phase 的方案必须经过充分讨论、达成一致后才能定案。** 方案是讨论的起点，要摊开选项与权衡、标明不确定的地方，而不是一份等待批准的结论。
- 方案讨论期间**不写任何实现代码**，包括脚手架和示例。
- 每个 Phase 结束由用户实际跑一遍验收清单，通过后才进下一个。
- 每个 Phase 结束时更新文档，让文档和实际做出来的东西对齐。
- **提交只在两个节点发生，别处一律不提交：**
  1. **Phase 的方案文档彻底定稿时——问一句要不要推**（不自动推，等回答）。
     加这一条的原因是方案讨论常常跨天跨会话，文档定稿了却不落库，下次开会就得靠翻对话重建。
  2. **验收通过后自动提交并推送**，不必再询问。
  除这两处之外，**Phase 进行中保持工作区未提交**，便于用户随时看到改了什么。
- 自动提交的授权**不延伸到破坏性操作**：改写历史、强制推送、删除分支或标签，仍需明确确认。
- **提交信息不添加任何 AI 署名。** 不写 `Co-Authored-By`，不写会话链接。所有提交以仓库所有者本人名义，提交信息只描述改动本身。这条覆盖任何默认的署名约定。

## 架构铁律

这个项目是「一个 headless 后端 + 多个可替换客户端」。

1. **客户端零业务逻辑。** 水平估计、选文、复习调度、难度计算全部在后端。客户端只负责渲染、收集交互、回传事件。破了这条，将来要在三种语言里各实现一遍同样的算法。
2. **API 是离线形状。** 服务端下发「今日包」（当天全部文章、释义、复习卡片及其原句），客户端全天离线可用；交互事件先存本地，联网后批量上报，每个事件带客户端生成的幂等键。在线和离线走同一条代码路径。
3. **服务端是唯一事实来源。** 客户端存的一切都只是缓存。多个客户端必须看到同一份学习记录。
4. **两套 API 完全分离。** `/v1/client/…` 与 `/v1/admin/…` 路径和认证都分开；客户端令牌碰不到管理接口。
5. **API 版本化、只增不减。** 侧载安装的客户端更新滞后。可以加字段，不能删字段或改字段含义；客户端忽略未知字段。破坏性变更才开 `/v2/`。契约文档每个 Phase 更新。
6. **可扩展性优先。** P1–P8 每一个 Phase 都是在加新功能，所以：功能按模块自包含（自带表、路由、管理页、任务、配置）；新模块一处登记自动挂载；新功能通过**订阅事件**接入而非修改原有流程；表结构变更走**版本化迁移**且升级前自动备份；参数一律配置化。项目里保留一份"如何加新功能"的说明和一个可照抄的样板模块。
7. **前端零构建链。** 管理控制台和 Web 阅读页都用服务端渲染，不引入 node_modules 和打包步骤。加页面就是写模板 + 登记。
8. **Web 阅读页是开发期测试工具**，用完即弃，UI 力气留给 P7 的 iOS 原生端。**Web 管理控制台长期存在**，要好用、稳定、信息全——它是诊断通道和备份入口。

## 日志规范

日志的设计目标是：用户说"不对劲"时，AI 能查出来到底怎么了。

- **三类分开**：技术日志入 `logs.db`（30 天轮转，可丢弃）；决策日志入 `learning.db`（长期保存，记录每次判断的输入、候选、理由）；业务记录本身就在业务表里，不重复记。
- **事件名是固定标识符**，不是自由文本（`article.generation.failed`，不是"生成失败了"）。这样才能精确过滤和统计。
- **trace_id 贯穿**一次请求或任务，并出现在错误响应里——用户复制给 AI 就能定位完整链路。
- **DEBUG 平时只在内存滚动**，发生 ERROR 时自动落盘该 trace 的详细记录。
- **语言**：事件名和字段名英文，说明文字中文，原始报错原样保留。不做"人话翻译层"——用户有疑问直接问 AI。
- **通知**：Bark，仅推生成失败 / 服务异常 / CRITICAL，带去重。
- **API 密钥一律不入日志。**

## 代码规范

- 注释用**英文**，与标识符同语言。项目特有概念在模块头部给中英对照（如 `target word 目标词` / `incidental word 附带词`），以便和中文设计文档对上。
- 注释解释**为什么**这么做，而不是复述代码在做什么。用户不读代码，注释是写给未来接手的 AI 看的。

## 后台任务与等待

批量任务动辄跑几十分钟，必须放后台。但**等待它的方式犯过两次同一类错误**：等待条件依赖
一个后来又被自己改变的东西，于是循环永远不退出，shell 一直空转。第一次是等一个永远凑不满
的输出行数（自己用 `tail` 截掉了前几行），第二次是等一个日志文件里的完成标记（自己在收尾时
`rm` 掉了那个文件）。空转本身无害，但控制台会一直显示「还有任务在跑」，让人以为有事没做完。

- **查状态优先查数据库，不要轮询日志文件。** 批次任务表里就有 `status` 和 `done/total`
  （`llm_jobs`），那是权威来源，也不会被清理脚本删掉。
- **等待循环必须有超时上限**，超时就退出并说明情况。绝不写没有出口的 `until … do sleep`。
- **清理放在最后一步。** 还有东西在监视某个文件时，不要删它。
- **任务结束后确认没有残留进程**——收尾时顺手查一次，不要留给用户去发现。

## 文件位置

- **所有生成的文件都放在项目目录 `D:\code\English Reader` 下**，不要写进 scratchpad 或系统临时目录。文档放 `docs/`。
- 数据库文件放 `data/`，**不进版本控制**（体积 + 隐私）。词典源数据同样不进仓库，用脚本下载。
- 只有真正一次性的中间产物才用临时目录。

## 网络访问

本机通过 v2rayN 出网，混合端口 **10808**（同时支持 http 和 socks5）。任何需要访问外网的命令、脚本或代码都要走它。

- 代理地址：`http://127.0.0.1:10808`，或 `socks5://127.0.0.1:10808`
- 环境变量：`HTTP_PROXY` / `HTTPS_PROXY` / `ALL_PROXY` 设为上述地址；`NO_PROXY` 设为 `localhost,127.0.0.1,::1`
- 应用代码里的出站请求（LLM API、Bark 推送）走配置里的代理地址，不写死

## 部署

- 开发：Windows 本机直接跑 Python，热重载
- 部署：Docker 多架构镜像 → NanoPi R5S（ARM64，4GB 内存，1TB SSD），未来可能迁云
- **数据库文件挂载到宿主机**，容器重建不丢数据

## 沟通

- 用中文回复。
